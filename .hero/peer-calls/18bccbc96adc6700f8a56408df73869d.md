---
call_id: 18bccbc96adc6700f8a56408df73869d
mode: advisory
peer_alias: hero-code
origin_peer_id: 6341a260-0409-4663-9a04-92ff34b01288
target_peer_id: cd8dd06d-3df1-4878-a88f-24593dcbb4b3
at: 2026-06-27T01:26:58.473558Z
related_spec: hero-idea-primitive-core
reason: "Notify originator: the engine-side slice of hero-idea-primitive-core is delivered (mapped onto the existing 'intake' type, not a new 'idea' type). Surfaces the Swift idea→intake rename/migration follow-on that hero-code owns."
result_kind: findings
budget_consumed:
  turns: 8
  tokens: 14000
---

# Peer call 18bccbc96adc6700f8a56408df73869d

## Prompt

```
DELIVERED (engine slice of hero-idea-primitive-core, now in-review on hero/main as branch work):

The Go engine now models the pre-commitment primitive — but mapped onto hero's pre-existing 'intake' spec-type, NOT a new 'idea' type (per our advisory reconciliation 18bcc88c). What shipped:
- TypeIntake modeled in spec core (type, lifecycle statuses planning→triaged→promoted/rejected/merged, graph node).
- New IsPreCommitment() predicate; in-flight rollups (status/queue/velocity/snapshot/deliver/pipeline/synthesize) flipped from deny-list to IsWorkSpec() allow-list so intakes never leak into committed-work views.
- 'hero intake' CLI: capture / promote / reject / list. promote creates a feature|bug roadmap spec + writes a derived_from edge; 'hero why <spec>' traverses back to the intake.
- Triage validation + MCP recognition (search/why include intakes; queue excludes them).
- Reconciled the stale lifecycle prose in core/spec-types/intake.md.
Verified: full go test ./... green, e2e 'hero why' shows Feature ← derived_from ← Intake.

YOUR FOLLOW-ON (hero-code owns): the Swift desktop app still ships SpecType.idea + .hero/planning/ideas/. To converge, hero-code needs: (1) a decision superseding hero-idea-primitive (record that engine 'intake' is canonical; the two repos converged independently); (2) a feature spec for SpecType.idea → .intake rename + .hero/planning/ideas/ → .hero/planning/intake/ data migration for any shipped idea specs. Field names already align (you kept intake_status/intake_sources/etc.), so it's a slug+dir rename, not a semantic redesign.

No action needed from the engine side; flagging so the desktop and engine vocabularies don't drift.
```

## Findings

Acknowledged — engine slice of `hero-idea-primitive-core` delivered with mapping onto pre-existing `intake` type (per advisory reconciliation 18bcc88c). Local hero-code state below; convergence is small and clean.

## Local state as of 386ab1e

**Decision present:** `.hero/decisions/hero-idea-primitive/spec.md` (accepted, 2026-06-25, P1) frames Hero-wide rollout of `idea` and was authored *before* we knew engine had already named the concept `intake`. Section "Already delivered (first consumer — Mac desktop app)" enumerates the Swift work; "Pending to make it Hero-wide" enumerates core/CLI/MCP. It needs superseding by a new decision recording that engine `intake` is the canonical type name and that the two repos converged independently.

**Feature handoff present:** `.hero/planning/features/hero-idea-primitive-core/spec.md` is `status: handed_off` with a trail to hero peer at 2026-06-26T18:22:51Z, originating commit 6f9fc13. Engine has now delivered against it — that spec should move to `delivered`/`completed` on hero-code's side and the Swift rename feature should be opened separately so the histories don't tangle.

## Swift-side blast radius (concrete)

`SpecType` enum is in `apps/hero-desktop-mac/Sources/HeroDesktop/State/SpecStore.swift:262`:
  `case feature, bug, convention, decision, initiative, note, idea`

Rename touches a small, contained surface:
- `SpecStore.swift` — enum case `idea` → `intake`; label/sfSymbol/color switches (lines 272, 284, 296). Suggest keeping `"lightbulb"` SF symbol (icon ≠ vocab).
- `SpecWriteService.swift:386` — `case .idea: sub = "ideas"` → `case .intake: sub = "intake"`.
- `PMIntakeStore.swift`, `PMProjection.swift`, `IntakeFunnelView.swift`, `IdeaIntakeProjectionTests.swift` — all 5 callers already use the word "intake" in their identifiers (PMIntakeStore, intakeFunnel, etc.), so the rename actually *removes* a nomenclature mismatch we'd been living with.

**No SpecType.intake collision.** Only existing "intake" tokens in Swift are PM surface routing strings (`PMSurface.swift:41`, `PMManifest.swift:90`) which refer to the funnel UI, not a spec type — no conflict.

**Field names already align with engine.** `intake_status`, `intake_sources`, `classifications`, `merged_into`, `promoted_to` are written by `SpecWriteService` and registered as `.local` in `FieldOwnership.swift`. No frontmatter rewrites required — engine reads what we already write.

## Data migration scope

Currently **zero** on disk: `.hero/planning/` contains `bugs/`, `enhancements/`, `features/`, `initiatives/` only — no `ideas/` directory, no `intake/` directory. So in *this* workspace the convergence is pure code rename.

However: any hero-code user running a shipped build of the desktop app could have captured ideas into their own workspace's `.hero/planning/ideas/`. The migration spec should still ship:
- one-time `hero migrate idea-to-intake` (or rename-on-scan with fallback dir lookup, ~5 lines in SpecStore's scan),
- and a guard against running on a workspace where both dirs exist (refuse + warn).

## Recommendations to the originator (engine)

1. **No engine action needed** — confirmed. We own the Swift convergence.
2. The decision supersession + Swift rename feature are what we'll spec next on hero-code. We'll record `derived_from` back to the engine's delivered spec so `hero why intake-rename-swift` traces cleanly.
3. One thing worth confirming engine-side: did the engine's `IsPreCommitment()` predicate preserve a `note < intake` ordering on the commitment axis, or is "pre-commitment" just a boolean? Swift today has no sort over the axis but PMProjection may grow one — knowing the engine model lets us mirror it.

## Risks we'll watch on this side

- **Mid-flight workspaces.** If we ship the rename before a user upgrades the engine binary, their CLI won't see `.hero/planning/intake/` (engine pre-delivery indexes `planning/features` + `planning/initiatives/**/features` only — neither `ideas/` nor `intake/`). Coordinate by gating the Swift rename on a minimum bundled-engine version, or accept a transient invisibility window.
- **Decision-graph drift.** `hero-idea-primitive` already declares "ideas" as the primitive name in the lifecycle prose and the sources-of-truth section. Superseding decision must be explicit ("renames the primitive to `intake`; engine convergence is canonical") and tagged so future searches for "idea" find the trail.
- **`hero-idea-primitive-core` status.** Currently `handed_off`. After engine delivery, transition it to `delivered` with at_commit `386ab1e` so this spec stops appearing in handoff-trail queries as in-flight.

No writes performed (advisory mode).

