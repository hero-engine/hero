---
title: "Work transfer: \"Jira attachment content is blocked by broker redirect policy\""
slug: mail-3ab437af53c66997e94a0268
type: intake
status: promoted
created: 2026-07-30
tags: []
source:
  kind: "mail"
  id: "mail_3ab437af53c66997e94a0268"
  sender_peer_id: "cd8dd06d-3df1-4878-a88f-24593dcbb4b3"
  recipient_peer_id: "6341a260-0409-4663-9a04-92ff34b01288"
  thread_id: "mail_3ab437af53c66997e94a0268"
promoted_to: mail-3ab437af53c66997e94a0268
relations:
  - target: mail-3ab437af53c66997e94a0268
    kind: promotes_to

---
# Work transfer: "Jira attachment content is blocked by broker redirect policy"

## Signal

{"origin_slug":"jira-attachment-content-redirect-blocked","reason":"Hero owns the Jira tracker broker and typed evidence downloader. Live production-path evidence confirms default attachment GETs fail as unsafe_redirect to api.media.atlassian.com, while the same authenticated request with redirect=false returns HTTP 200 and the attachment body. Preserve the same-origin credential boundary; normalize Jira attachment reads to redirect=false and add broker/evidence regressions. Hero Code will consume the returned contract and bundle the corrected binary.","source_commit":"99eb0eb","source_peer_id":"cd8dd06d-3df1-4878-a88f-24593dcbb4b3","summary":"---\ntitle: \"Jira attachment content is blocked by broker redirect policy\"\nslug: jira-attachment-content-redirect-blocked\ntype: bug\nstatus: planning\ndiagnosis_status: diagnosed\ndomain: engineering\nsurface: hero-desktop-mac\nsize: medium\npriority: P1\nseverity: high\nroot_cause_class: design\ncreated: 2026-07-30\nrelations:\n  - target: brokered-tracker-agent-access\n    kind: related\n  - target: desktop-brokered-tracker-tools\n    kind: related\n  - target: chat-tracker-evidence-tool-gap\n    kind: related\n  - target: lazy-tracker-evidence-sidecar\n    kind: related\ntags: [jira, attachments, tracker-broker, redirects, credentials, desktop]\n---\n\n# Jira attachment content is blocked by broker redirect policy\n\n## Kickoff\n\nPreserve the tracker broker's cross-origin credential boundary while making Jira Cloud\nattachment bodies readable through the configured Jira origin.\n\n**Status:** planning — diagnosis is confirmed; `redirect=false` returned the real body\nthrough bundled Hero, and no source code was changed.\n\n**Pick up at:** normalize Jira attachment-content GETs in Hero, then add engine and\ndesktop contract regressions.\n\n→ `/deliver jira-attachment-content-redirect-blocked`\n\n**Files:** `../hero/internal/tracker/broker.go`, `../hero/internal/tracker/jira.go`,\n`packages/hero-swift/Sources/HeroSharedApplication/Engine/AgentLoop.swift`\n**Skip:** do not allowlist `api.media.atlassian.com` or forward Jira authorization across\norigins.\n\n## Summary\n\n### Categorization\n\n| Attribute | Assessment |\n|---|---|\n| **Criticality** | High — Jira issue metadata remains readable, but attachment bodies needed as source evidence are systematically unavailable through the advertised non-local research path |\n| **Ease of Fix** | Moderate — the safe request shape already works, but the contract must be made deterministic in the Hero engine and documented in Hero Code |\n| **Caused by our codebase?** | Yes — the broker's safety check is correct, but our Jira integration does not encode the provider-specific request needed to avoid Jira's cross-origin redirect |\n| **Needs more research?** | No — the failing and successful request shapes were both reproduced against the same real attachment through the bundled Hero binary |\n\n### Background\n\nA Hero Code chat could read a Jira issue, its comments, and attachment metadata. Ten\nattempts to read attachment bodies with\n`GET rest/api/3/attachment/content/\u003cattachment-id\u003e` failed when Jira returned a redirect\nto `api.media.atlassian.com`. The tracker broker rejected each redirect as\n`unsafe_redirect`, after which the agent reported that uploaded source documents were\ninaccessible.\n\nAtlassian documents a `redirect` query parameter for this endpoint: with\n`redirect=false`, Jira returns `200 OK` and streams the content from the configured Jira\norigin instead of returning the media redirect:\n\u003chttps://developer.atlassian.com/cloud/jira/platform/rest/v3/api-group-issue-attachments/\u003e.\n\n### Analysis\n\nThe cross-origin rejection is an intentional credential boundary, not the defect. Hero's\ngeneric broker request injects the Jira credential, permits only same-origin redirects,\nand maps an attempted media redirect to `unsafe_redirect`. Hero Code correctly uses this\nbroker for a non-local issue because the `evidence` operation requires an already-imported\nlocal spec.\n\nThe defect is the missing provider contract between those two correct decisions. Neither\nthe broker nor the native tool instructions state or enforce `redirect=false` for Jira\nattachment-content requests. The model therefore issued the documented endpoint's\ndefault request ten times and deterministically hit the safety boundary.\n\n### Root Cause\n\nThis is a **design** defect. We expose a generic same-origin request operation for Jira\nresearch, but leave a security-relevant Jira transport detail to model discovery.\n`GET /rest/api/3/attachment/content/{id}` defaults to a cross-origin media redirect,\nwhereas the broker deliberately forbids all cross-origin redirects. The existing request\ncontract already supports structured query parameters, so\n`query_params: {\"redirect\":[\"false\"]}` is sufficient; our code and instructions simply\ndo not make that request shape automatic or discoverable.\n\nThe fix must preserve the current cross-origin check. Allowlisting the media host would\nbroaden the credential boundary and is unnecessary.\n\n## Problem Statement\n\nWhen an agent researches a Jira issue that has not been imported as a local Hero spec,\nattachment metadata is visible but attachment bodies are unreadable. The only advertised\npath is `hero_tracker.request`; its default Jira attachment request returns a 303 to a\ndifferent origin, which the credential broker correctly rejects. The agent receives a\nnon-retryable failure even though Jira can return the same bytes safely from the original\norigin when asked not to redirect.\n\n## Environment Details\n\n- Hero Code macOS session:\n  `/Users/developer/.config/hero-code/sessions/5246D907-F7AB-4307-B2B3-C0C6D9B82169/messages.json`\n- Session evidence: lines 1566–1656 contain ten attachment-content calls and\n  `unsafe_redirect` results; lines 1660–1669 contain the resulting user-facing blocker.\n- Hero engine: bundled executable from\n  `apps/hero-desktop-mac/dist/Hero.app/Contents/Resources/hero`\n- Tracker: Jira Cloud through the configured `morph-jira` connection.\n- The system `hero` binary was not used for the successful probe because it reported a\n  schema mismatch; the app-bundled binary is the authoritative runtime for this path.\n\n## Reproduction and Evidence\n\n1. Use `hero_tracker.get_issue` to read a non-local Jira issue with attachments.\n2. Call `hero_tracker.request` with `GET` and\n   `rest/api/3/attachment/content/\u003cattachment-id\u003e`.\n3. Observe a `tracker-broker/v1` response with `error.code: unsafe_redirect` and a\n   redacted redirect target on `api.media.atlassian.com`.\n4. Repeat the same request through the bundled Hero binary with\n   `\"query\":{\"redirect\":[\"false\"]}`.\n5. Observe `status_code: 200`; the bounded response body begins with the real attachment\n   content. No cross-origin request or authorization forwarding is required.\n\nThe successful live probe used the same connection and attachment as the failing probe.\nIts body and signed URL are intentionally omitted from this spec.\n\n## End-to-End Code Trace\n\n1. `AgentLoop.swift:4681-4701` advertises `hero_tracker.request` for bounded same-origin\n   endpoints, but gives no Jira attachment instruction.\n2. `ProjectContext.swift:483-492` repeats the same generic guidance.\n3. `ToolExecutor.swift:1021-1080` maps `query_params` to the broker's `query`, serializes\n   the request, and launches bundled `hero tracker broker request`. This mapping is\n   correct and already supports the safe workaround.\n4. `../hero/internal/cli/tracker_broker.go:15-63` dispatches JSON stdin to\n   `tracker.Broker.Request`.\n5. `../hero/internal/tracker/broker.go:349-379` resolves the configured Jira URL, injects\n   authentication, and rejects any redirect for which `sameOrigin(next.URL, origin)` is\n   false.\n6. `broker.go:384-390` classifies that rejection as `unsafe_redirect`.\n7. Jira's default attachment endpoint redirects to its media origin, so the request never\n   reaches body reading at `broker.go:392-400`.\n8. Supplying `redirect=false` is preserved by `ToolExecutor` and `brokerRequestURL`; Jira\n   returns the body from the configured origin and the existing bounded/redacted response\n   path succeeds.\n\n## Confirmed and Disproven Claims\n\n| Claim | Status | Evidence |\n|---|---|---|\n| The failure is caused by the broker losing credentials | Disproven | The broker injects auth before the request; failure occurs in `CheckRedirect` before the media host is contacted |\n| Jira attachment bodies require a cross-origin allowlist | Disproven | The same real request returned 200 with `redirect=false` |\n| Hero Code cannot express the safe request | Disproven | `ToolExecutor` already maps structured `query_params` to the broker query |\n| The local `evidence` helper should have been used | Disproven for this report | That operation requires an exact local `spec_slug`; the issue was intentionally non-local research |\n| The system Hero binary proves production behavior | Disproven | It had a schema mismatch; the successful verification used the packaged app runtime |\n| Missing Jira transport normalization is the root cause | Confirmed | Default request fails reproducibly; the single supported query change succeeds reproducibly |\n\n## Secondary Defects\n\n`../hero/internal/tracker/jira.go:823-850` claims that the typed evidence downloader\n\"preserves the same-origin boundary,\" but it validates only the initial URL and then uses\nthe default HTTP redirect behavior. The Go client normally strips sensitive headers when\nredirecting to an unrelated host, so this is not evidence of credential disclosure.\nHowever, the implementation and its comment do not enforce the same fail-closed redirect\ncontract as the broker. The same `redirect=false` request shape and an explicit\nsame-origin redirect check should be applied there so both attachment paths have one\nauditable policy.\n\n`../hero/internal/tracker/evidence_store.go:365-371` reduces typed attachment download\nfailures to `attachment \u003cid\u003e: download unavailable`. That omission is outside the\nreported non-local path, but a regression test should ensure transport failures remain\ndiagnosable without leaking signed URLs.\n\n## Goal\n\nMake Jira attachment bodies reliably readable through both brokered non-local research\nand typed local evidence while retaining the existing same-origin credential boundary.\n\n## Acceptance Criteria\n\n- When a Jira attachment-content GET is sent through the tracker broker, the system shall\n  request `redirect=false` and return the bounded body from the configured Jira origin.\n- If Jira still attempts a cross-origin redirect, the system shall continue to return\n  `unsafe_redirect` and shall not contact the redirect target.\n- When the typed Jira evidence downloader fetches an attachment, it shall use\n  `redirect=false` and enforce the same-origin redirect boundary.\n- Hero Code's tracker instructions shall explicitly describe the safe Jira attachment\n  request and shall forbid media-host allowlisting or raw authenticated Bash fallback.\n- Existing same-origin redirects, output bounds, redaction, and non-idempotent redirect\n  rejection shall remain unchanged.\n\n## Suggested Fix Approach\n\n### 1. Normalize Jira attachment-content requests in the broker\n\n**File/function:** `../hero/internal/tracker/broker.go`,\n`(*Broker).Request` and a focused path predicate.\n\n**Before (current code):**\n\n```go\ntarget, origin, err := brokerRequestURL(r.connection, in.RelativePath, in.Query)\nif err != nil {\n\treturn finishResponse(failResponse(resp, \"unsafe_request\", redact.apply(err.Error()), false), start, b.now)\n}\nif err := validateCallerHeaders(in.Headers); err != nil {\n\treturn finishResponse(failResponse(resp, \"unsafe_headers\", err.Error(), false), start, b.now)\n}\n```\n\n**After:**\n\n```go\ntarget, origin, err := brokerRequestURL(r.connection, in.RelativePath, in.Query)\nif err != nil {\n\treturn finishResponse(failResponse(resp, \"unsafe_request\", redact.apply(err.Error()), false), start, b.now)\n}\nif r.connection.Provider == \"jira\" \u0026\u0026\n\tmethod == http.MethodGet \u0026\u0026\n\tisJiraAttachmentContentPath(target.Path) {\n\tquery := target.Query()\n\tquery.Set(\"redirect\", \"false\")\n\ttarget.RawQuery = query.Encode()\n}\nif err := validateCallerHeaders(in.Headers); err != nil {\n\treturn finishResponse(failResponse(resp, \"unsafe_headers\", err.Error(), false), start, b.now)\n}\n```\n\n```go\nfunc isJiraAttachmentContentPath(path string) bool {\n\tconst prefix = \"/rest/api/3/attachment/content/\"\n\tid := strings.TrimPrefix(path, prefix)\n\treturn id != path \u0026\u0026 id != \"\" \u0026\u0026 !strings.Contains(id, \"/\")\n}\n```\n\n**Why:** This turns a Jira-specific safety requirement into deterministic provider\nbehavior. It uses the configured origin and leaves the existing redirect rejection\nuntouched","title":"\"Jira attachment content is blocked by broker redirect policy\"","type":"bug"}

## Notes

<!-- Triage thoughts, links, who asked. Promote with `hero intake promote`. -->
