# Delivery audit — hero-v034-release-prep

**Audited:** `git diff 253560c4 -- .github/workflows/release.yml .hero/planning/initiatives/hero-marketing/hero-v034-release-prep/spec.md docs/releases/v0.34.0-candidate.md docs/releases/v0.34.0-launch-checklist.md release/third-party/go-BSD-3-Clause.txt scripts/release_candidate.py scripts/test_release_candidate.py internal/releasecontract/release_contract_test.go`
**Verdict:** HOLD
**Surface:** noteworthy

## Acceptance criteria

- [✓] AC-1: Target `v0.34.0` from the latest `v0.33.0` release authority — `source_identity` accepts only canonical semantic versions, requires the baseline to be the latest release tag, rejects an existing target tag, requires a clean checkout, and records the source commit/tree (`scripts/release_candidate.py:111-145`). Preserved provenance identifies baseline `v0.33.0`, revision `fcc72b4988b479abe733c87ee0bfb6bd686830ff`, and tree `fdb2d69a08c85b05d020c0da5b2075a269ce95b0` (`.build/release-candidate/v0.34.0/provenance.json`).
- [✗] AC-2: Produce reproducible packages, checksums, SBOM, notices, and provenance — the preserved candidate contains all five target archives plus checksums, CycloneDX 1.5 SBOM, notices, and provenance; all eight listed checksums verify, all archives contain the exact same notice packet, and the builder byte-compares two complete builds (`scripts/release_candidate.py:358-417,475-492`). However, `--output` may resolve to any repository child and an existing target is recursively deleted (`scripts/release_candidate.py:487-489,503-507`). In a clean checkout, a valid invocation such as `--output .git/objects` or `--output internal` passes the guard and can destroy repository state. The DONE claim is downgraded until output is constrained to a dedicated candidate subtree or replacement is made non-destructive.
- [✗] AC-3: Align candidate release claims with the public registry — the notes correctly label the individual memory and delivery systems as shipped, integrations and focused packs as optional, the headless runtime as preview, and Hero Code/Hero Cloud/Sprout boundaries (`docs/releases/v0.34.0-candidate.md:43-53`). They also describe Hero as two connected systems (`docs/releases/v0.34.0-candidate.md:9-15`) without stating that the reinforcing two-system loop is **preview**, contrary to the claim registry and authoritative public copy (`.hero/specs/hero-public-truth-baseline/public-claim-registry.yaml:47-58`; `web/docs/src/index.md:27-30`). The named maturity test does not assert this required label (`scripts/test_release_candidate.py:90-101`).
- [✓] AC-4: Complete clean local candidate installs without private source access — `smoke_candidate` extracts the native archive, checks the stamped version, initializes a fresh Git repository, isolates HOME/XDG state, installs the Codex target, runs `status` and `check`, and asserts installed artifacts (`scripts/release_candidate.py:428-472`). The supplied exercise evidence records that this journey passed against the Darwin ARM64 archive from the preserved candidate.
- [✓] AC-5: Do not tag, upload, or publish without final approval — manual dispatch can run only the candidate job and has no artifact upload step; the publishing job is tag-ref-only and requires root `LICENSE` plus root `THIRD_PARTY_NOTICES.txt` (`.github/workflows/release.yml:25-63,76-91`). Neither root file exists, no `v0.34.0` tag exists, and the audited delivery contains no tag, release, visibility, or deploy mutation.

## Changes

- [✗] Derive the `v0.33.0..candidate` inventory and write maturity-aligned release notes — the candidate notes summarize the 13 non-merge commits and preserve most capability/product boundaries, but omit the registry's preview label for the reinforcing two-system loop (`docs/releases/v0.34.0-candidate.md:9-53`).
- [✓] Reconcile target metadata, channels, manifests, and public release references — the builder stamps `v0.34.0`, names the five expected target archives, preserves `v0.33.0` as the published baseline, and keeps manual candidate verification separate from tag-only publication (`scripts/release_candidate.py:24-30,118-145,327-390`; `.github/workflows/release.yml:25-63`).
- [✗] Build supported artifacts reproducibly with release evidence — artifact inventory, byte comparison, SHA-256 verification, 19 non-root SBOM components (17 reviewed modules plus Go and model), and the 65,200-byte exact-license notice packet are present. The output deletion boundary remains unsafe (`scripts/release_candidate.py:487-489,503-507`). The immutable Sprout pseudo-version in `go.mod:7` is correctly treated as a module version rather than a tag requirement; its exact module ZIP contains a root MIT `LICENSE`, and it is correctly excluded from the release-binary SBOM because `cmd/mock-tracker-server` is not in the shipped binary closure.
- [✓] Exercise installs and document destinations, upgrade, and rollback — the candidate notes distinguish binary replacement from workspace regeneration and direct operators through `hero doctor` then `hero upgrade` (`docs/releases/v0.34.0-candidate.md:55-63`); the checklist covers pre-publication cleanup, immutable-tag handling, channel rollback to `v0.33.0`, irreversible visibility exposure, and docs/landing rollback (`docs/releases/v0.34.0-launch-checklist.md:65-80`). Supplied validation records the native smoke, public docs/landing contracts, and 28 docs plus 8 landing tests as passing.
- [✓] Produce the exact approval/launch checklist without publication — the checklist names all nine candidate outputs, resolves preparation checks as PASS, and leaves all ten human-controlled Apache/visibility/release mutations explicitly GATED (`docs/releases/v0.34.0-launch-checklist.md:7-63`). Current GoReleaser configuration does not yet place `LICENSE` or notices into production archives; the checklist correctly retains that as a hard final-launch action (`docs/releases/v0.34.0-launch-checklist.md:49-52`), while the workflow fails closed if either root file is absent (`.github/workflows/release.yml:76-79`).

## Open items

- None recorded. Every Completion Ledger row is marked DONE; AC-2/Change 3 and AC-3/Change 1 are downgraded by this audit.

## Audit notes

- Constrain resolved candidate output to a dedicated `.build/release-candidate/` descendant and add rejection tests for repository root, `.git`, tracked source directories, symlink escapes, and outside-repository paths; alternatively stage and atomically replace only a validated candidate directory.
- Add the exact `preview` boundary for the reinforcing memory/delivery loop to the candidate notes and make `test_candidate_notes_preserve_public_maturity_and_product_boundaries` assert it.
- Final production publication remains correctly gated, not delivered: before any tag, add the approved root Apache `LICENSE`, promote root notices, configure GoReleaser archives to include both files, rebuild from the licensed revision, and complete the visibility/channel checks.
