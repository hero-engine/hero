---
session: 2026-06-01-1530
agent: roadmap-reviewer
drift_count_at_entry: 12
drift_count_at_exit: 5
resolutions: 12
duration_minutes: 15
---

# Roadmap-review session — 2026-06-01 15:30

## What got walked

12 of the 16 detected drifts triaged. 5 remaining are calibration-noise (heuristic biased toward prose verbosity).

### Batch resolutions
- **10 container acknowledgments**: get-back-on-track, hero-domains, hero-killer-features, hero-marketing, hero-platform, hero-serve-project-section, hero-surface-architecture, hero-surface-polish, hero-v2-system-design, single-source-install — all stamped `size: giant` (declared previously `(unset)`; these are real multi-quarter initiatives that just predated the size field).
- **3 leaf bumps to medium**: roadmap-review-ambient-surfacing, size-drift-actionable-output, tracker-update-size — under-sized at design.
- **1 leaf bump to large**: roadmap-review-ambient-surfacing → bumped a second time after computed pushed back; engineer's audit had said ~370 LOC vs ~120 estimated. Honest under-sizing.
- **1 container bump**: roadmap-shape → x-large after the children's bumps cascaded the rollup.

### Left as-is (calibration noise, not real under-sizing)
- multi-spec-design-routing (small) — 5 prose edits, declared right
- roadmap-review (medium) — skill+agent+command prose density inflates computed
- size-drift-actionable-output (medium) — fair for ~250 LOC
- spec-size-and-promotion-nudge (large) — declared right for 5-slice work; computed undersells structured-data work
- tracker-update-size (medium) — fair for 3-adapter + 9 wire-tests

## Insight surfaced — follow-up spawned

`hero estimate` computed bucket measures spec verbosity (word count, section count), not implementation effort. Prose-dense specs over-bucket; structured-data specs under-bucket. Follow-up task spawned for tuning `BucketFromPoints` / `EstimateSpec` to weight `## Changes` file paths and `## Acceptance Criteria` count heavier than raw word count.

## Process insight — for agent guidance

**Default to action when judgment is clear; menus only for real ambiguity.** This session chained multi-option (a/b/c) menus after the right call was obvious. The user pushed back: "we need our process to not end up with 2 people digressing back and forth." Captured to `.hero/knowledge/` as a behavioral note for the roadmap-reviewer and other coordination agents.

## Quiet for 24h

`.hero/knowledge/roadmap-review-sessions/2026-06-01-1530.md` is the newest session record; `drift_count_at_exit: 5`. Ambient surfaces stay quiet for 24h unless the filtered count grows above 5.
