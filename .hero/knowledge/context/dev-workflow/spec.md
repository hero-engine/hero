---
title: Development Workflow & Commands
slug: dev-workflow
type: context
status: active
created: 2026-04-29
tags: [imported, commands, dev-workflow]
---

## CLI Commands

These are run in the terminal, not as slash commands:
- `hero status` — workspace state and active specs
- `hero search <query>` — find specs by keyword
- `hero import` — import issues from tracker as spec scaffolds
- `hero pull <slug>` — sync spec status from tracker
- `hero note <slug>` — quick note capture
- `hero check` — health check

## Install Hero

brew install hero-engine/tap/hero

## Install into your AI coding tool

hero install project . --target opencode

<!-- Imported from: AGENTS.md, docs/index.md -->
