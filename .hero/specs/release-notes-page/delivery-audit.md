# Delivery audit — release-notes-page (re-audit)

**Audited:** working tree (uncommitted) — `git status --short` + `git diff` on tracked files, `Read` on new/untracked files
**Verdict:** SHIP
**Surface:** noteworthy (re-audit of a prior HOLD; carries forward the verification detail for the three items that were previously blocked)

## Re-audit of prior HOLD findings

1. **Pagination bug — VERIFIED FIXED, not a band-aid.**
   `web/docs/scripts/generate_release_notes.py:143-176` — `fetch_releases()`
   now loops `page=N`, requesting `per_page=100` each call, and only stops
   when a page comes back shorter than 100 items. This is a real
   until-short-page loop, not a fixed `per_page=100` cap that would
   silently re-break above 100 releases. Confirmed by reading the
   implementation directly, not the docstring or the ledger's prose.
   - Ran `python3 -m unittest test_generate_release_notes.py -v` myself
     from `web/docs/scripts/`: **16/16 pass**, including the two new tests.
   - Read `test_fetch_releases_pages_past_first_page_of_100` (lines
     180-217): mocks 3 pages (100/100/43 = 243 items), asserts
     `len(releases) == 243`, `urlopen.call_count == 3`, and that the
     requested pages were exactly `["1","2","3"]`. This is a genuine
     non-truncation assertion, not a "no exception raised" test.
   - Read `test_fetch_releases_requests_max_page_size` (lines 167-179):
     asserts `per_page=100` and `page=1` appear in the first request URL.
   - Confirmed on disk: `grep -c '^## v' web/docs/src/releases/index.md`
     → **43**, matching the engineer's live-run claim exactly.
   - ✓ Fixed with real evidence, independently reproduced.

2. **Missing visual-regression ledger row — VERIFIED, independently
   re-run.** I had browser tooling available in this environment, so the
   engineer's claim did not need to be taken on faith. Ran
   `mkdocs build --strict` locally in `web/docs/` — clean, no warnings.
   Served the built site, loaded `/cli/overview/` and `/concepts/core-loop/`
   in a real browser, and screenshotted both: left nav renders the
   integrated TOC correctly on each, active-link items show the
   Hero-blue highlight, no visual regression on either page. Also loaded
   `/releases/` itself and confirmed the integrated TOC lists every
   release version in the left nav.
   - ✓ Fixed with evidence generated independently in this audit, not
     just the engineer's description.

3. **AC#9 keyboard-navigable — VERIFIED, independently re-run.** Also
   had browser tooling available. Confirmed via JS introspection on the
   live `/releases/` page: 87 `.headerlink` permalink anchors and ~127
   `.md-nav--primary .md-nav__link` entries are real `<a href>` DOM
   elements in the natural tab order (not `div`/`span` fakes). Focused a
   `.headerlink` programmatically and confirmed a visible focus outline
   (`outline-style: auto`, `outline-color: rgb(108, 182, 255)` — Hero
   blue) is applied, i.e. focus-visible styling is real, not just the
   element being technically reachable. Sent real `Tab` keypresses and
   confirmed focus moved through the page correctly. Note: the ledger
   cited "134 nav links"; I independently measured ~127 — within noise
   of DOM state (collapsed/expanded nav sections shift the count) and
   not a materially different claim; the headerlink count (87) matched
   the ledger exactly.
   - ✓ Fixed with evidence generated independently in this audit.

All three prior HOLD items are genuinely fixed, not re-asserted claims.

## Acceptance criteria (full pass)

- [✓] Renders `/releases/` page listing every release — 43/43 releases present, confirmed on disk and in browser.
- [✓] Left-hand nav lists each release by version/date — integrated TOC (`toc.integrate`, `web/docs/mkdocs.yml:41`), confirmed in browser screenshot.
- [✓] Clicking a nav entry scrolls to its section — anchor hrefs (e.g. `#v0241-2026-07-12`) verified to match heading-derived permalink IDs, generated by the pre-existing `toc.permalink` config.
- [✓] Groups notes into Major Features/Fixes for post-Change-1 releases — `.goreleaser.yaml:47-53` adds `changelog.groups`; `parse_release_body()` recognizes exactly the two group titles (`_KNOWN_GROUP_TITLES`, script line 55), asserted by `test_grouped_parsing`.
- [✓] Ungrouped fallback for pre-Change-1 releases — verified in the live-rendered page: all 43 current releases render under a single `### Changes` heading (expected — no release has shipped under the new `changelog.groups` config yet), matching `test_ungrouped_falls_back_to_single_section` and `test_default_goreleaser_changelog_heading_falls_back_to_changes`.
- [✓] Deep-linkable anchor per release — verified live (see above).
- [✓] Mobile drawer collapse — inherited, unmodified mkdocs-material behavior; not re-screenshotted at mobile width in this round (no code in this diff touches it), and it was already accepted in the prior audit round. Not re-litigated.
- [✓] Regenerates automatically on every docs deploy — `.github/workflows/docs.yml` runs `generate_release_notes.py` before `mkdocs build --strict` (diff confirmed).
- [✓] Fully keyboard-navigable — re-verified independently this round (see item 3 above); no longer a disclosed partial.

## Changes (full pass)

1. `.goreleaser.yaml` — `changelog.groups` added exactly as spec'd (`Major Features` / `^feat`, `Fixes` / `^fix`), existing `filters.exclude` untouched. ✓
2. `web/docs/scripts/generate_release_notes.py` (new) — matches spec'd behavior; pagination is now correct (see re-audit item 1). ✓
3. `web/docs/mkdocs.yml` — `toc.integrate` (correct substitution for the spec's nonexistent `navigation.integrate`, already validated in the prior audit round) + `Releases: releases/index.md` nav entry. ✓
4. `.github/workflows/docs.yml` — generator step added before `mkdocs build --strict`, correct working directory (`web/docs`), `GITHUB_TOKEN` passed through. ✓
5. `.github/workflows/release.yml` — `actions: write` permission added; `gh workflow run docs.yml --field deploy=true` step added, gated on `if: vars.DEPLOY_DOCS == 'true'`. Cross-checked against `docs.yml`'s `workflow_dispatch.inputs.deploy` (exists, boolean, default `true`) — field name and gate logic line up. `release.yml` only fires the docs workflow when `DEPLOY_DOCS` is on; `docs.yml`'s deploy step then honors `inputs.deploy` unconditionally on `workflow_dispatch`. Net effect: a tag push never force-deploys docs when `DEPLOY_DOCS` is off, matching the stated Risk. ✓
6. `web/docs/src/stylesheets/brand.css` — no diff; the ledger's "verified, no changes needed" matches `git diff` showing nothing. Independently confirmed visually in the browser (Hero-blue active styling intact on `.md-nav__link--active` for both the releases page and the two spot-checked existing pages). ✓

## Open items

None outstanding.

## Audit notes

- Minor doc staleness (not a delivery defect): the spec's "Implemented
  in" bullet for the test file says "14 unittest cases"; the actual
  suite now has 16 (the two pagination tests added in this fix pass
  weren't reflected in that prose). Cosmetic only — doesn't affect the
  verdict.
- No `## Completion Ledger` section exists in `spec.md` itself — the
  ledger referenced by spec prose ("see Completion Ledger note...") and
  by the engineer's fix-pass was supplied to this audit out-of-band, not
  read from the file. Not scored against the verdict (the underlying
  code/test/build/browser evidence is what was actually verified), but
  worth folding the ledger into the spec file before this is archived,
  so the delivery record is self-contained.
- Scope stayed tight to the spec's named files in `## Changes`; no drift
  observed in `git status --short`.
