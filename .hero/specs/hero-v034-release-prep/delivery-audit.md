# Delivery audit — hero-v034-release-prep

**Audited:** `git diff 253560c4 -- .goreleaser.yaml .github/workflows/release.yml .hero/planning/initiatives/hero-marketing/hero-v034-release-prep/spec.md docs/releases/v0.34.0-candidate.md docs/releases/v0.34.0-launch-checklist.md release/third-party/go-BSD-3-Clause.txt scripts/release_candidate.py scripts/test_release_candidate.py internal/releasecontract/release_contract_test.go` at `ea5386c6e3e55e2657ed279caf0dcef157be0845` (tree `2d0ee55873e22fcba41f3e13cff836d02fd0f69b`)
**Verdict:** SHIP
**Surface:** clean

## Acceptance criteria

- [✓] AC-1 — `source_identity` accepts only canonical semantic versions, requires the supplied baseline to be the latest semantic release tag, rejects an existing target tag, and records the exact commit/tree (`scripts/release_candidate.py:119-153`). The candidate provenance records baseline `v0.33.0`, target `v0.34.0`, revision `ea5386c6e3e55e2657ed279caf0dcef157be0845`, and tree `2d0ee55873e22fcba41f3e13cff836d02fd0f69b`; the repository's latest release tag is `v0.33.0` and `v0.34.0` is absent.
- [✓] AC-2 — the builder generates five supported target archives twice, compares complete output hashes, normalizes archive metadata, emits checksums/provenance, renders a CycloneDX 1.5 SBOM, and assembles exact license texts (`scripts/release_candidate.py:166-230`, `scripts/release_candidate.py:233-290`, `scripts/release_candidate.py:305-425`, `scripts/release_candidate.py:483-500`). The preserved candidate has five archives, a 19-component SBOM (17 reviewed modules plus Go and the embedded model), a 65,200-byte notices packet, and all recorded checksums pass. Destructive replacement is confined to a symlink-resolved child below `.build/release-candidate` (`scripts/release_candidate.py:97-102`, `scripts/release_candidate.py:495-497`); tests reject the repository root, release root, `.git/objects`, tracked `internal`, an outside path, and symlink escape (`scripts/test_release_candidate.py:17-43`).
- [✓] AC-3 — candidate notes identify project memory and verified delivery as shipped while explicitly keeping their measurable/reinforcing improvement loop in preview, and preserve optional/headless/product/license boundaries (`docs/releases/v0.34.0-candidate.md:9-56`). The regression test asserts both the shipped components and both preview-loop formulations plus the Hero Code, Hero Cloud, Sprout, optional-workflow, and unpublished boundaries (`scripts/test_release_candidate.py:118-132`), matching the public capability registry (`web/docs/src/reference/capability-status.md:11-37`, `web/docs/src/reference/capability-status.md:67-124`).
- [✓] AC-4 — the smoke path extracts the host archive, verifies its stamped version, initializes a fresh Git repository, uses isolated HOME/XDG state, installs Codex content, runs `status` and `check`, and asserts installed files (`scripts/release_candidate.py:436-480`). The clean detached candidate run reports `smoke: true`; the supplied exercise evidence records the Darwin ARM64 artifact completing that journey without a source checkout or private-repository access.
- [✓] AC-5 — manual dispatch is isolated to the unpublished candidate job and writes only provenance/checksums to the job summary; it contains no binary uploader (`.github/workflows/release.yml:25-60`, `scripts/test_release_candidate.py:108-116`). Publishing is tag-only and fails closed if root `LICENSE` or `THIRD_PARTY_NOTICES.txt` is absent (`.github/workflows/release.yml:62-91`); GoReleaser will pack both after those gates land (`.goreleaser.yaml:26-35`). Both root files and the `v0.34.0` tag are currently absent, consistent with the supplied evidence that no upload, release, visibility change, or deployment occurred.

## Changes

- [✓] Change 1 — the `v0.33.0..candidate` history is reconciled into memory-first positioning, Engineering/PM/QA composition, focused packs, public trust/onboarding, maturity boundaries, and upgrade guidance without marketing release mechanics as product features (`docs/releases/v0.34.0-candidate.md:3-79`).
- [✓] Change 2 — the candidate builder stamps `v0.34.0` into binaries and artifact names (`scripts/release_candidate.py:335-416`); public history remains on published `v0.33.0`; workflow dispatch is candidate-only while publication remains tag-driven (`.github/workflows/release.yml:3-18`, `.github/workflows/release.yml:25-91`). GoReleaser's Homebrew/Scoop metadata stays version-derived and now includes the future final license/notices in production archives (`.goreleaser.yaml:23-35`, `.goreleaser.yaml:58-95`).
- [✓] Change 3 — Darwin amd64/arm64, Linux amd64/arm64, and Windows amd64 outputs are deterministically archived, reproduced, and accompanied by checksums, SBOM, notices, and exact provenance (`scripts/release_candidate.py:24-30`, `scripts/release_candidate.py:305-425`, `scripts/release_candidate.py:483-500`). The final local candidate contains every named artifact; every copied checksum verifies.
- [✓] Change 4 — the artifact smoke is an isolated clean-install journey (`scripts/release_candidate.py:436-480`); candidate notes distinguish binary replacement from `hero upgrade` (`docs/releases/v0.34.0-candidate.md:58-66`), and the checklist documents pre-publication, channel, tag, visibility, and site rollback (`docs/releases/v0.34.0-launch-checklist.md:65-80`). Supplied validation records public docs/invocations, 28 docs tests, and 8 landing tests passing.
- [✓] Change 5 — the launch checklist names all nine candidate artifact IDs, resolves all preparation rows as PASS, and leaves exactly ten human-controlled launch mutations explicitly GATED with concrete actions (`docs/releases/v0.34.0-launch-checklist.md:7-63`, `scripts/test_release_candidate.py:134-148`). It includes no tag or upload action performed during preparation.

## Open items

- None. The Completion Ledger contains no PARTIAL, SKIPPED, or BLOCKED rows.

## Audit notes

- Validation evidence is complete: 12 Python candidate tests passed; focused release-contract Go tests and the full post-fix `go test ./...` passed (`/tmp/hero-v034-release-go-test-final.log`); GoReleaser configuration and workflow YAML checks passed; public docs/invocation, docs (28), and landing (8) tests passed; spec drift and lint each passed 5/5; score is 95/A; copied candidate checksums pass.
- The candidate remains deliberately unpublished and license-gated. Final Apache grant, root license/notices promotion, candidate rebuild from the licensed revision, public visibility, tag, release, channels, and deployments remain launch-gate work, not delivery gaps in this preparation spec.
