# Delivery audit — core-commands-domain-neutral

**Audited:** working tree vs base commit `752e516` (uncommitted changes; `git diff 752e516 -- core/ domains/engineering/commands/drive.md`)
**Verdict:** SHIP
**Surface:** clean

## Acceptance criteria

- [✓] No unscoped agent/skill/command ref in `core/commands/*.md` — `grep -rn "architecture-reviewer\|dependency-analyst\|brownfield-architect\|greenfield-architect\|product-ideator\|feature-delivery-lead\|platform-delivery-lead\|pm-reviewer\|pm-delivery-lead" core/commands/` returns 9 hits across `decide.md`, `check.md`, `retro.md`, `discover.md` — all read the actual file content and confirmed every hit sits inside "engineering:"/"engineering ships"/"pm:"/"on engineering installs" scoping. Independently confirmed `pm-reviewer`, `pm-delivery-lead` exist at `domains/pm/agents/`, `product-ideator`/`architecture-reviewer`/`dependency-analyst`/`brownfield-architect`/`greenfield-architect`/`feature-delivery-lead`/`platform-delivery-lead` exist at `core/agents/` or `domains/engineering/agents/`.
- [✓] `/hero` routes via install's instruction-file table — read `core/commands/hero.md` post-edit in full: no hardcoded workflow list remains; routing logic reads "the routing table in this install's instruction file (the managed region of AGENTS.md / CLAUDE.md...)"; `hero do` CLI pointer retained.
- [✓] `/decide` cites `.hero/knowledge/decisions/<slug>.md` — read `core/commands/decide.md` line 16 directly: "Save it to `.hero/knowledge/decisions/<slug>.md` using the decision (ADR) template from the `spec-format` skill." Confirmed `spec-format` skill genuinely has a "Decision spec template (ADR)" section (`core/skills/spec-format/SKILL.md:232`).
- [✓] `/convention` and `/check conventions` cite `.hero/knowledge/conventions/` — read both files directly: `convention.md:13` saves to `.hero/knowledge/conventions/<slug>.md`; `check.md:18` loads from `.hero/knowledge/conventions/`. `grep -rn "\.hero/decisions\|\.hero/conventions" core/commands/` returns empty (independently re-run).
- [✓] Exactly one session-start command (`/resume`) with reconcile + session-primer pointer — `core/commands/prime.md` confirmed absent (`ls` errors "No such file or directory"); read `core/commands/resume.md` in full: step 7 (`hero check --reconcile`), step 8 (team-mode NEXT.md glance), closing `session-primer` pointer all present. Confirmed `session-primer` is a real core agent (`core/agents/session-primer.md`).
- [✓] pm/sales installs ship no `/drive` or `/prime` — `core/commands/drive.md` and `core/commands/prime.md` both confirmed absent from the repo source; `domains/pm/`, `domains/sales/` diffs against base are empty (nothing added there either), so neither domain pack independently reintroduces the files. (Ledger's install-smoke claim not independently re-run — see Audit notes.)
- [✓] Engineering install ships `/drive` unchanged from pre-move core copy — independently ran `git diff -M --find-renames` and a byte-level `diff` between `git show 752e516:core/commands/drive.md` and the current `domains/engineering/commands/drive.md`: **identical**, 0 lines changed, 100% similarity index confirmed by git's own rename detection.
- [✓] `core/commands/handoff.md` cites skills by name only — read the file in full: closing paragraph reads "See the `next-md` skill... and the `kickoff-prompt` skill..."; `grep -rn "skills/.*\.md" core/commands/` returns empty (independently re-run). Confirmed both `next-md` and `kickoff-prompt` skills genuinely exist under `core/skills/` (kickoff-prompt promotion from the sibling spec is real, not assumed).
- [✓] Shadowing domain files still carry non-empty `core_fork:` and differ from core — independently ran `go test . -run TestDomainPacks_NoUnannotatedCoreShadows -v`: PASS for engineering/sales/pm subtests. Confirmed the only two `core_fork:` shadows in the repo are `domains/pm/commands/discover.md` and `domains/pm/commands/handoff.md` (both pre-existing, both untouched — `git diff 752e516 -- domains/pm/` is empty).

## Changes

- [✓] 1. `core/commands/hero.md` — slim the meta-router — diff confirms the "Available workflows" section (14-command + CLI lists) and hardcoded intent table are deleted; replaced with instruction-file-table deferral; `hero do` pointer and "Important" rules retained.
- [✓] 2. `core/commands/check.md` — conditional delegation + conventions path — both the convention-compliance and dependency branches now read "delegate...if one is installed (engineering ships X); otherwise perform...directly"; conventions path is `.hero/knowledge/conventions/`; general/stale/drift branches confirmed untouched in the diff (only 2 hunks + trailing summary line touched).
- [✓] 3. `core/commands/decide.md` — neutral evaluation + decisions path — step 1 (`hero_anchor`) kept verbatim; hard route replaced with direct-or-delegate framing scoped `engineering:`/`pm:`; architect consults scoped "On engineering installs..."; save path updated.
- [✓] 4. `core/commands/convention.md` — conventions path — single-line diff, only the save path changed, nothing else touched.
- [✓] 5. `core/commands/discover.md` — agent fallback + handoff target — "using the `product-ideator` agent" replaced with install-conditional framing; output framing now points to "install's design workflow"; confirmed `domains/pm/commands/discover.md` (the pm `core_fork:` override) is untouched.
- [✓] 6. `core/commands/retro.md` — delivery-lead fallback — preamble replaced with conditional delegation; engineering/pm names scoped in a follow-up sentence; steps 1–6 confirmed verbatim in the diff (only the opening paragraph hunk changed).
- [✓] 7. Move `core/commands/drive.md` → `domains/engineering/commands/drive.md` — confirmed via `git diff -M --find-renames --stat` (0 insertions/deletions, pure rename) and a byte-level `diff` against the pre-move blob from `752e516` (identical).
- [✓] 8. `core/commands/resume.md` — absorb prime's unique steps — steps 7 (`hero check --reconcile`) and 8 (team-mode NEXT.md glance) added, plus closing `session-primer` pointer; rest of file confirmed untouched in the diff.
- [✓] 9. Delete `core/commands/prime.md` — confirmed absent via `ls` (No such file or directory) and `git status` shows `D  core/commands/prime.md`.
- [✓] 10. `core/skills/knowledge-flywheel/SKILL.md` — retarget `/prime` — line 18 diff confirms "When `/prime` detects..." → "When `/resume` detects...". `grep -rn "/prime" core/ domains/` independently re-run: empty.
- [✓] 11. `core/commands/handoff.md` — skill references by name — closing paragraph confirmed rewritten to name both skills with no `skills/<name>.md` path forms; no "where installed" hedge present, consistent with the ledger's claim that the sibling spec (`pm-pack-phantom-surfaces`, commit `65e3333`) already promoted `kickoff-prompt` to core before this delivery started (independently confirmed `core/skills/kickoff-prompt/` exists).

## Open items (if any)

- **`hero docs check` — SKIPPED** — Engineer's stated reason: pre-existing structural failure (`findProjectRoot()` resolves to this repo's git root, which has no top-level `agents/`/`commands/`/`skills/` dirs — this repo uses `.agents/`), independent of this spec's content, plus no literal count claim ("core 17→15") exists in README.md/GETTING-STARTED.md to update.
  - **Assessment: concrete, independently verified.** Built the binary and ran `hero docs check` in the current worktree: exit code 1, `agents claims 34, actual 0` / `skills claims 45, actual 0`, actual counts all zero. Then built the same binary from a clean worktree at base commit `752e516` (before any of this spec's edits) and ran it there: **byte-identical output** — same exit code, same two mismatches, same zero counts. Read `internal/cli/docs_check.go` and `internal/cli/root.go:232` (`findProjectRoot`) directly: confirmed the function walks up from cwd for `.hero/` or `.git/`, and `countMDFiles`/`countSkillDirs` look for `agents/`, `commands/`, `skills/` at that root — none of which exist at this repo's root (only `.agents/` does). Also independently confirmed no "17→15" / "13→14" style count claim exists in README.md or GETTING-STARTED.md. The reasoning in the ledger is accurate and the skip is legitimate scope discipline, not an excuse.

## Audit notes

- No downgrades. Every DONE row in the ledger was checked against actual file content (not just grep summaries) and matched.
- Scope is clean: `git status --short` shows exactly the spec file, the 9 in-place edits, 1 deletion (`prime.md`), and 1 rename (`drive.md`) — nothing else touched. `domains/pm/` and `domains/sales/` diffs against base are empty, confirming no drive-by edits and no new `core_fork:` shadows.
- Full test suite independently re-run (`go build ./...` then `go test ./...`): zero FAIL lines, every package `ok` or `[no test files]`. `TestDomainPacks_NoUnannotatedCoreShadows` independently re-run with `-v`: PASS for all 3 domain subtests (engineering, sales, pm).
- One thing not independently re-verified: the ledger's three-way `hero install --domain {pm,sales,engineering} --root` smoke test (fresh temp-dir installs, spot-checking that `pm-reviewer`/`pm-delivery-lead`/`session-primer` resolve in the installed `.claude/agents/`). This audit instead verified the underlying claims a different way — reading the actual source agent files directly (`domains/pm/agents/pm-reviewer.md`, `pm-delivery-lead.md` exist; `core/agents/session-primer.md` exists) and confirming the two files most likely to regress (`core/commands/drive.md`'s removal, `core/commands/prime.md`'s removal) via `ls`. This is judged sufficient corroboration; re-running the actual install smoke test would be redundant given the source-level checks all passed, but a stricter auditor could still choose to re-run it.
- Found a real-but-out-of-scope wrinkle the ledger didn't dwell on: `grep -rn "/prime"` across the whole repo (not scoped to `core/`+`domains/`) turns up two additional installed-artifact hits — `CLAUDE.md` (repo root) and `.agents/skills/knowledge-flywheel/SKILL.md` — both tracked files that are stale installed copies of this repo's own root harness install, last regenerated at `3e34f72`. This is the exact class of staleness the ledger's `hero docs check` note already flags and explicitly puts out of Boundaries scope ("No engine/Go changes beyond `git mv`"); it does not indicate scope creep or a missed edit, just confirms the ledger's own risk disclosure was accurate rather than optimistic.
