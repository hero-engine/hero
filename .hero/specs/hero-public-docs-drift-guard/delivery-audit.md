# Delivery audit — hero-public-docs-drift-guard

**Audited:** `git diff --cached 539cd09c -- .github/workflows/docs.yml .github/workflows/landing.yml README.md GETTING-STARTED.md web/docs/src/index.md web/docs/src/cli/code-host.md .hero/planning/initiatives/hero-marketing/hero-public-docs-drift-guard/spec.md internal/cli/docs_check.go internal/cli/public_docs_check.go internal/cli/public_docs_check_test.go internal/serve/mcp_tools_def.go`
**Verdict:** SHIP
**Surface:** clean

## Acceptance criteria
- [✓] AC-1: derive public inventory counts from canonical registries and distinguish MCP totals from filtered surfaces — `internal/cli/docs_check.go:93`, `internal/cli/public_docs_check.go:35`, `internal/serve/mcp_tools_def.go:855`, and `TestCanonicalMCPInventoryUsesRuntimeRegistryAndConfiguredProfiles` use the canonical install enumerator, runtime MCP definitions, and configured `ToolFilter`.
- [✓] AC-2: validate executable commands, clean-workspace quickstarts, and full configuration examples — `internal/cli/public_docs_check.go:104`, `:216`, `:388`, and `:481` validate fenced command paths, positional arguments, required flags/values, execute all three marked quickstarts in isolated Git repositories, and decode marked JSON through `config.Load`; `TestMarkedPublicQuickstartsRunInCleanWorkspaces`, `TestInvocationValidationRequiresArgumentsAndFlagValues`, `TestExecutablePublicCommandMutationRejectsMissingArguments`, and `TestPublicConfigExamplesUseProductionDecoder` are biting tests.
- [✓] AC-3: scan every scoped public surface for high-risk contradictions — `internal/cli/public_docs_check.go:321` walks all supported text sources under `web/docs/src` and `web/landing/site`, adds MkDocs navigation, root public docs, and rendered install guidance; `TestPublicNarrativeSurfaceDiscoveryIncludesNavigationReleasesAndAssets` proves navigation, releases, notices, metadata, assets, landing, and rendered guidance are discovered, while `TestPublicNarrativeMutationsAreRejected` exercises the contradiction rules.
- [✓] AC-4: expose and verify source revision and timestamp — `internal/cli/public_docs_check.go:535` validates both revision templates and landing metadata; `:576` requires exact source revision parity and RFC3339 `generated_at`; `.github/workflows/docs.yml:68` and `.github/workflows/landing.yml:60` stamp `${{ github.sha }}` before deployment.
- [✓] AC-5: crawl deployed landing/docs surfaces and fail on stale reviewed-source parity — `internal/cli/public_docs_check.go:576` checks canonical roots, key docs paths, revision endpoints, HTTP availability, exact revision, and timestamp format; `TestProductionCrawlChecksPagesAndExactRevision` and `TestProductionCrawlFailsClosedWhenSurfaceIsUnavailable` cover success, mismatch, and unavailable paths; both workflows invoke the matching crawl only after their approved deploy step.
- [✓] AC-6: preserve generated-command-reference scope — `internal/cli/docs_check.go:185` retains the existing `--invocations` pass and composes it with `--public`; the supplied fresh-binary exercise resolved every documented invocation while running the public contract.
- [✓] AC-7: fail on dependency, availability/DNS, anonymous source-link, and repository-boundary drift — `internal/cli/public_docs_check.go:65`, `:368`, `:505`, and `:576` enforce dependency majors, fail closed on HTTP/DNS errors, gate private source links, require Hero Code/Cloud proprietary separation, require Sprout's separate public MIT status outside Hero's future grant, and reject Sprout/Apache coupling; dependency, boundary, narrative-mutation, and unavailable-surface tests prove the checks bite.

## Changes
- [✓] Derive install and MCP inventories — existing `canonicalInstallCounts` remains authoritative; `serve.MCPToolDefinitions` and configured runtime filtering produce total/default/profile MCP counts.
- [✓] Validate command semantics, configuration, and quickstarts — fenced commands now enforce arguments and flag values, marked JSON uses the production decoder, and README, Getting Started, and hosted-doc quickstarts run `init → install → status/check` in isolated repositories and assert installed artifacts.
- [✓] Scan public sources for contradictions — the scanner covers root public docs, every supported hosted-doc and landing text asset, navigation, releases, notices, metadata, and rendered AGENTS guidance; discovery coverage prevents silent omission of the previously skipped families.
- [✓] Build, link, revision, and illustrative-output checks — the public gate composes with existing docs and landing Python suites, strict MkDocs build, sanitizer, JavaScript/link checks, source/build/artifact validation, and workflow YAML validation.
- [✓] Tie deployments to revision and crawl after deployment — `.github/workflows/docs.yml:68` and `.github/workflows/landing.yml:60` stamp the exact workflow SHA, preserve existing approval gates, and invoke surface-specific production parity checks after deployment.
- [✓] Enforce dependency, DNS/source-link, and Hero/Sprout/proprietary boundaries — compatibility-bounded MkDocs pins, fail-closed production requests, private-source gating, positive Hero Code/Cloud and Sprout separation requirements, and forbidden Sprout grant claims are implemented with mutation coverage.

## Open items
- None.

## Audit notes
- No performative ledger rows, partial items, skips, blockers, or out-of-scope implementation changes were found.
- The supplied focused Go log (`/tmp/hero-docs-drift-go-test.log`) and full-suite log (`/tmp/hero-v034-full-go-test.log`) are clean. The supplied fresh-binary exercise passed with 35 agents, 29 commands, 57 skills, 82 MCP tools, all invocations, and all three isolated quickstarts. Docs/landing suites and builds, sanitizer, JavaScript, links, artifact checks, workflow YAML parsing, drift 7/7, lint 7/7 EARS, and score 95/A were also reported clean. No live deployment or production-network crawl was performed; local HTTP fixtures cover the production parity success and fail-closed behavior, and deployment remains outside this delivery's authorization.
