# Delivery audit — interactive-setup-and-connect-closure

**Audited:** `git diff 155d8c7...70c6279` (repair: `git diff 73fac3a...70c6279`)
**Verdict:** SHIP
**Surface:** clean

## Acceptance criteria

- [✓] **AC-1 — TTY provider selection from the registry** — `runConnect` gates on `prompt.IsInputTTY`, renders `connectProviderNames`, and uses the selected provider (`internal/cli/connect.go:98-128`). Built-CLI PTY and registry-equivalence coverage is in `TestConnectProviderPickerAsksAtATerminal`, `TestConnectProviderPickerOffersEveryProvider`, and `TestConnectProviderOptionsComeFromTheProviderMap` (`internal/cli/prompt_setup_commands_test.go:50`, `:78`, `:107`).
- [✓] **AC-2 — only applicable missing fields and valid roles are asked** — the private provider-scoped field table and skip-present collector are in `internal/cli/connect.go:515-560,640-663`; role choices are derived through `ValidateProviderRole` at `:702-726`. Tests cover the provider role sets, PTY role selection, and paired path persistence (`internal/cli/connect_writer_unification_test.go:203`, `:401`, `:425`).
- [✓] **AC-3 — closed/live non-TTY fails before I/O or mutation** — the full collector gate precedes banner, role, field, network, and writer calls (`internal/cli/connect.go:571-613`). `TestInteractiveConnectFailsFastOnAnUnansweredStream` and the open-writer `io.Pipe` regression assert bounded return and zero output (`internal/cli/connect_writer_unification_test.go:343`, `:356`).
- [✓] **AC-4 — JSON does not prompt** — provider selection and the interactive collector refuse JSON before prompting (`internal/cli/connect.go:101-103,576-580`); install's existing JSON gates remain in place. PTY JSON coverage is present in `TestConnectProviderPickerRefusesUnderJSON` and `TestInteractiveConnectNeverPromptsUnderJSON` (`internal/cli/prompt_setup_commands_test.go:170`; `internal/cli/connect_writer_unification_test.go:314`).
- [✓] **AC-5 — one validated connection writer** — both collectors call `writeConnection`, which validates role/settings and owns capability, selector, default, and persistence resolution (`internal/cli/connect.go:271-280,301-410,613-622`). `TestConnectHasExactlyOneCommittedWriter` plus the persistence tests guard the contract (`internal/cli/connect_writer_unification_test.go:203`, `:284`, `:389`).
- [✓] **AC-6 — explicit role uses the flag collector and rejects early** — `cmd.Flags().Changed("role")` participates in routing, and provider/role validation precedes token reads (`internal/cli/connect.go:116-127,229-249`). `TestRoleFlagRoutesToTheFlagDrivenPath` and the Jira rejection test assert no input consumption/persistence (`internal/cli/connect_writer_unification_test.go:99`; `internal/cli/connect_integrations_test.go:162`).
- [✓] **AC-7 — equivalent interactive/flag persistence** — both routes construct `connectionInput`; `TestBothPathsPersistIdenticalState` compares committed and local files across all providers/roles (`internal/cli/connect_writer_unification_test.go:203`).
- [✓] **AC-8 — code-host resolver acceptance without default theft** — code-host capability and non-default selection are resolved at `internal/cli/connect.go:358-371`; both path-specific resolver tests load persisted config and call `ResolveCodeHostConnection` (`internal/cli/connect_writer_unification_test.go:129`, `:166`).
- [✓] **AC-9 — failed config patch leaves no credential** — configuration is patched before local/global credential persistence (`internal/cli/connect.go:373-397`); `TestConnectFailsWhenHeroJSONCannotBeWritten` asserts no committed config, local config, or credential file (`internal/cli/connect_writer_unification_test.go:493`).
- [✓] **AC-10 — one ordered six-target install/uninstall set** — uninstall help, validation, picker, and dispatch use the existing `installTargets` list (`internal/cli/uninstall.go:33-93`). Rendered-picker parity and exact order are asserted by `TestUninstallTargetPickerOffersAllSixTargets` and `TestInstallAndUninstallPickersEnumerateIdenticalTargets` (`internal/cli/prompt_setup_commands_test.go:246`, `:276`).
- [✓] **AC-11 — safe Copilot/generic manifest removal** — the repaired removers delete only their target-owned `.github/` or `.ai/` surfaces and leave shared root `AGENTS.md` untouched (`internal/cli/uninstall.go:253-347`). `TestUninstallCopilotAndGenericRoundTripsPreserveAdjacentUserFilesAndAGENTS` performs real installs for both targets, reads the generated manifest, uninstalls, asserts every target-prefixed manifest path is gone, and verifies an adjacent user file plus the complete post-install `AGENTS.md` bytes survive (`internal/cli/uninstall_test.go:38-98`).
- [✓] **AC-12 — named setup shortfalls prompt only at a TTY** — `promptableArgs` relaxes only a TTY shortfall (`internal/cli/prompt_args.go:30-40`), and repos/users/trust collect only absent values. The built-CLI matrix covers full, partial, empty, non-TTY, supplied, and surplus cases (`internal/cli/prompt_setup_commands_test.go:395-531,567-683,690-803,852-888`).
- [✓] **AC-13 — passwords use secure terminal input** — `users add` and `users passwd` continue through `promptPassword`, which wraps `prompt.Secret` and refuses ordinary streams (`internal/cli/users.go:131-144,174-222`). The stream-refusal and existing password-prompt coverage exercise both commands (`internal/cli/prompt_setup_commands_test.go:646`; `internal/cli/prompt_baseline_test.go:229-237`).
- [✓] **AC-14 — satellite confirmations honor Cobra streams and suppression gates** — the predecessor implementation uses `prompt.Confirm`, TTY checks, default-no behavior, and dry-run/JSON suppression in `internal/cli/install.go:526-528,595-611` and `internal/cli/install_satellites.go:129-130,237-269`; its satellite/baseline tests remain in the passing focused and full suites.
- [✓] **AC-15 — both connect help surfaces describe the delivered modes and flags** — both commands share `connectCmd.Long` and `addIntegrationConnectFlags`; their project usage strings match (`internal/cli/connect.go:19-80`; `internal/cli/connect_alias.go:12-39`). `TestConnectHelpSurfacesExposeTheSameFlags` and `TestConnectAliasIntegrationFlagsEquivalent` inspect the registered surfaces (`internal/cli/connect_help_test.go:10`; `internal/cli/connect_integrations_test.go:59`).
- [✓] **AC-16 — fully supplied path compatibility is captured and preserved** — `TestSuppliedSetupCompatibilityBaseline` runs all four original uninstall targets, repos add, users add with `--password`, and both trust targets through the built binary and compares command, exit status, stdout, and stderr byte-for-byte against eight checked-in fixtures (`internal/cli/prompt_baseline_test.go:345-394`; `internal/cli/testdata/prompt_baseline/supplied_*.flags.txt`). Existing uninstall, repos, users, and trust behavior tests supply the corresponding mutation evidence.

## Changes

- [✓] **1 — capture the supplied-path contract** — `internal/cli/prompt_baseline_test.go` now enumerates all eight required supplied invocations and compares them to checked-in byte fixtures under `internal/cli/testdata/prompt_baseline/`.
- [✓] **2 — replace divergent connect writers with two collectors and one writer** — implemented in `internal/cli/connect.go`, with early role validation, a connect-private field table, whole-collector TTY/JSON gates, and config-before-credential ordering.
- [✓] **3 — focused connect regressions** — `internal/cli/connect_writer_unification_test.go` covers role routing, one writer, paired state, both resolver paths, JSON, failed config writes, valid roles, closed streams, and a genuinely live open pipe.
- [✓] **4 — adopt only named flat setup prompts** — `internal/cli/prompt_args.go`, `internal/cli/repos.go`, `internal/cli/users.go`, and `internal/cli/trust.go` contain the bounded adoption; satellite confirmation behavior was already present and retained. Structural coverage rejects descriptor reuse.
- [✓] **5 — six-target parity with manifest-safe Copilot/generic removal** — target parity uses `installTargets`; Copilot/generic target-owned removers preserve the shared root instruction file and are exercised through real manifest-producing installs (`internal/cli/uninstall.go:33-93,253-347`; `internal/cli/uninstall_test.go:38-98`).
- [✓] **6 — truthful connect help on both surfaces** — shared long help/flag registration and matching project usage are implemented in `internal/cli/connect.go` and `internal/cli/connect_alias.go`, with `internal/cli/connect_help_test.go` coverage.
- [✓] **7 — complete setup and target-parity matrix** — `internal/cli/prompt_setup_commands_test.go` covers the picker/setup matrix, while `internal/cli/uninstall_test.go:38-98` adds the missing Copilot/generic install→uninstall manifest and preservation round trips.
- [✓] **8 — bounded compatibility documentation** — the conditional release-note surface does not exist in the base or audited tree; delivered behavior is documented in the updated connect help without importing unrelated donor notes.

## Audit notes

- The bounded repair directly closes both prior HOLD groups: it removes shared-root mutation from the two new uninstallers, adds real manifest round trips, and adds all eight supplied-path byte fixtures.
- The supplied focused CLI suite, full normal suite, CLI race suite, vet, native build, and Windows cross-build are reported passing.
- `git diff --check 155d8c7...70c6279` and `git diff --check 73fac3a...70c6279` are clean.
- Hero projection files in the diff are expected delivery metadata. The unrelated dirty `.hero/drive/interactive-cli-input-scoped-completion.json` file is outside the audited commit range and was not treated as evidence.
