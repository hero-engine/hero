# Delivery audit — github-pull-request-state-transition-broker

**Audited:** `git diff 07395c5...635337f`
**Verdict:** SHIP
**Surface:** clean

## Acceptance criteria

- [✓] Expose four explicit external-write transitions — `internal/codehost/broker.go:37` advertises `mark_ready`, `retarget`, `close`, and `reopen`; `internal/codehost/github_state_test.go:27` asserts their `external_write` and `explicit_user` policies.
- [✓] Require qualified PR, head, intent, idempotency, and revisions — `contracts/codehostbroker/validate.go:99` and `contracts/codehostbroker/validate.go:110` require qualified identity and mutation safety fields; `contracts/codehostbroker/validate.go:691` and `internal/codehost/github_state.go:63` strictly validate lifecycle payloads; `TestStateTransitionRequiredFieldsAndScope` asserts zero writes when required fields are missing.
- [✓] Retarget requires observed current and explicit existing new base — `internal/codehost/github_state.go:98` binds both refs to the PR repository; `internal/codehost/github_state.go:183` exact-matches current repository/ref/SHA and resolves the explicit target at its requested SHA; `TestStateTransitionStalePermissionAndTargetGates` covers stale and missing targets.
- [✓] Reject incompatible permission, revision, head, lifecycle, and base changes before dispatch — `internal/codehost/github_state.go:154` implements provider-backed head, lifecycle, permission, current-base, and target-ref gates; the shared mutation state machine checks capability and observation revisions at `internal/codehost/reconcile.go:80`; `TestStateTransitionStalePermissionAndTargetGates` proves these failures perform zero writes.
- [✓] Exact desired state returns external completion — `internal/codehost/github_state.go:368` compares retarget repository/ref/SHA and keeps lifecycle comparators operation-specific; `TestStateTransitionExternallyCompletedAndMergedTerminal` proves all four operations return `externally_completed` with zero writes, and `TestStateTransitionDesiredStateMatrix` rejects a same-name target with a different SHA.
- [✓] Lost responses reconcile from the exact authoritative desired state — dispatch and detached reconciliation use the exact comparator at `internal/codehost/github_state.go:276` and `internal/codehost/github_state.go:287`; `TestStateTransitionLostAmbiguousDelayedAndCancellation` covers applied, delayed, and unavailable proof, while `TestStateTransitionStalePermissionAndTargetGates` proves post-write target movement remains `ambiguous`.
- [✓] Preserve merged as an immutable terminal state — `internal/codehost/github_state.go:162`, `internal/codehost/github_state.go:276`, and `internal/codehost/github_state.go:298` reject merged state before dispatch and during read-back; `TestStateTransitionExternallyCompletedAndMergedTerminal` exercises every operation against the merged fake.
- [✓] Same-key retries perform at most one provider transition — the connection-scoped journal and lock in `internal/codehost/reconcile.go:34` gate dispatch; `TestStateTransitionOperationsAdvertisedAppliedAndReplayed` and `TestStateTransitionDuplicateConcurrentConflictAndProviderRejection` assert one attempt for sequential and concurrent retries across all four operations.
- [✓] Changed operation, PR, head, base, or desired target conflicts — `internal/codehost/github_state.go:413` binds every named dimension into the canonical payload digest; `TestStateTransitionDuplicateConcurrentConflictAndProviderRejection` changes each dimension and asserts `idempotency_conflict` without another provider attempt.
- [✓] Return a new observation revision and invalidate prior readiness evidence — `internal/codehost/github_create.go:307` returns post-transition lifecycle evidence and typed `invalidated_operations: [get_merge_readiness]`; `contracts/codehostbroker/validate.go:529` bounds and validates the signal; `TestStateTransitionOperationsAdvertisedAppliedAndReplayed` asserts it for every transition, and the canonical consumer fixture carries it for all four operations.
- [✓] Respect pre- and post-dispatch cancellation boundaries — `internal/codehost/reconcile.go:89` stops before dispatch and `internal/codehost/reconcile.go:148` performs bounded detached reconciliation after an ambiguous dispatch; `TestStateTransitionLostAmbiguousDelayedAndCancellation` covers both boundaries for every operation.
- [✓] Exercise required transition hazards through the fake and fixture — `internal/mockcodehost/server.go:206` defines external completion, stale head/base, missing and moving branches, permission races, delayed visibility, merged state, denial, cancellation, and lost responses; `internal/codehost/github_state_test.go` exercises them, including the post-write target race, while `contracts/codehostbroker/contract_test.go:101` validates all four lifecycle fixture cases and their readiness invalidations.

## Changes

- [✓] Add validation and desired-state comparators — `contracts/codehostbroker/validate.go:691` and `internal/codehost/github_state.go:63` validate operation payloads; `internal/codehost/github_state.go:351` and `internal/codehost/github_state.go:368` implement distinct lifecycle states and repository/ref/SHA-exact desired-state comparison.
- [✓] Add lifecycle and target preflight — `internal/codehost/github_state.go:154` verifies identity, lifecycle, head, current base, target existence/SHA, actor, permission, capability, and observation evidence before dispatch.
- [✓] Implement typed GitHub transition routes — `internal/codehost/github_state.go:238` uses the typed GraphQL ready-for-review mutation and bounded REST patches for retarget, close, and reopen, then reads authoritative PR state.
- [✓] Extend journal and desired-state reconciliation — `internal/codehost/idempotency.go:64` adds content-free current/desired base and lifecycle receipt evidence; `internal/codehost/reconcile.go:34` reuses the locked dispatch/reconciliation machine; exact SHA comparison prevents post-write target movement from being reported as applied.
- [✓] Distinguish normalized lifecycle states — `internal/codehost/github_state.go:351` keeps merged, closed-unmerged, open-draft, open-ready, and unknown separate, with merged terminal checks at every safety boundary.
- [✓] Extend the deterministic GitHub fake — `internal/mockcodehost/server.go:206`, `internal/mockcodehost/server.go:660`, and `internal/mockcodehost/server.go:763` model mutable lifecycle state, typed provider routes, branch movement, permission changes, delayed visibility, cancellation, loss, and attempt accounting.
- [✓] Add effect, stale-state, receipt, invalidation, and conformance tests — `internal/codehost/github_state_test.go:27` through `internal/codehost/github_state_test.go:571` covers policy, exact state matrices, pre/post-write races, external completion, merged terminals, retries, conflicts, cancellation, journal safety, and typed invalidation; `contracts/codehostbroker/contract_test.go:101` and the byte-stable fixture cover the additive consumer contract.

## Audit notes

- None.
