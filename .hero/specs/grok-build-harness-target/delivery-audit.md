# Delivery audit — grok-build-harness-target

**Audited:** `e09f3ee` working tree via `git diff -- .hero/knowledge/tripwires/harness-changes-cover-all-targets/spec.md GETTING-STARTED.md README.md domains/engineering/AGENTS.md internal/cli internal/install`, plus direct reads of the required untracked spec, Grok target, and Grok test files
**Verdict:** SHIP
**Surface:** clean

## Acceptance criteria

- [✓] Project install creates native Grok artifacts — `internal/install/target_grok.go:13` installs Markdown agents, nested skills, command skills, and `AGENTS.md`; `TestGrokProjectInstallUsesNativeLayout` asserts `.grok/agents`, `.grok/skills`, `.grok/config.toml`, and the root instruction.
- [✓] Commands install as user-invocable skills without `.grok/commands` — `internal/install/target_grok.go:24` renders commands into the skills root through `commandAsSkillRenderer("Grok Build")`; `TestGrokProjectInstallUsesNativeLayout` and `TestGrokSatelliteLinksOnlyDeclaredDirectories` assert the workflow skill and absence of a commands tree.
- [✓] Shared renderer preserves Codex bytes and uses Grok wording — `internal/install/render.go:193` parameterizes only the harness preamble; `TestCommandAsSkillRendererPreservesCodexBytes` asserts Codex's exact output and `TestGrokProjectInstallUsesNativeLayout` checks Grok wording while rejecting Codex wording.
- [✓] Global install uses only `~/.grok` — `internal/install/target_grok.go:51`, `internal/install/agents_md.go:39`, and `internal/install/mcp.go:247` resolve global native paths; `TestGrokGlobalInstallUsesUserNativeLayout` asserts all global artifacts and no project-local `.grok` output.
- [✓] MCP is one valid portable entry and preserves non-Hero bytes — `internal/install/mcp.go:273` validates and byte-preservingly replaces Hero's managed TOML block with bare `hero`; `TestManagedTOMLMCPUpsertPreservesUserBytesAndConverges`, `TestManagedTOMLMCPUpsertRemovesDuplicateLegacyHeroTables`, and `TestRegisterMCP_CommandIsPortable_AllTargets` assert preservation, convergence, validity, deduplication, and portability.
- [✓] Workspace-root MCP args preserve sibling configuration — `internal/install/mcp.go:284` adds `--project-root` only inside Hero's block; `TestManagedTOMLMCPUpsertPreservesUserBytesAndConverges` asserts exact outer bytes, root args, valid TOML, and second-run identity.
- [✓] Malformed Grok TOML is refused without mutation — `internal/install/mcp.go:299` validates user-owned TOML before writing and `internal/install/install.go:194` returns Grok MCP errors; `TestGrokMalformedConfigIsRefusedWithoutMutation` asserts a useful parse error and byte-identical failure behavior.
- [✓] State, detection, doctor, auto-sync, and upgrade include Grok — `internal/install/install.go:35`, `internal/install/satellite.go:60`, `internal/install/auto_sync.go:65`, `internal/install/inventory.go:50`, `internal/cli/doctor.go:167`, and `internal/cli/upgrade.go:406` add the target to the relevant registries and resolution; project, inventory, doctor, and upgrade tests assert persisted state, native detection, commands-as-skills inventory, and explicit/default upgrade resolution.
- [✓] Upgrade and auto-sync refresh owned files while preserving user files — `internal/install/target_grok.go:32` uses manifest/prefix-safe skill pruning and shared file-manifest pruning runs from `internal/install/install.go:206`; `TestAutoSyncRefreshesGrokAndPreservesForeignContent`, `TestInstallLeavesForeignSkillDirsAlone`, and the all-seven-target `TestPruneStaleFiles_*` matrix assert refresh and provenance-safe preservation.
- [✓] Uninstall removes only owned artifacts and Grok state — `internal/cli/uninstall.go:348` removes manifest-owned agent/skill files, Hero's TOML block, and conditionally the shared instruction region; `internal/install/state.go:154` removes only the Grok state entry. Both Grok uninstall tests cover user files, config bytes, shared instructions, other-target preservation, and state removal; the reported isolated exercise removed 123 Hero files and preserved exactly two planted user fixtures.
- [✓] Workspace and satellite installs use declared Grok directories and root routing — `internal/install/satellite.go:23` gives Grok only `agents` and `skills`, with an `AGENTS.md` marker, while `internal/install/mcp.go:284` supports workspace-root arguments; `TestGrokSatelliteLinksOnlyDeclaredDirectories`, target-layout coverage, and the managed TOML root test assert the contract.
- [✓] Dry-run, force, overlays, repair, and orphan safety match existing contracts — Grok routes through shared install, render, integrity, prune, and satellite machinery; `TestGrokMCPDryRunDoesNotWrite`, `TestGrokInstallIsIdempotent`, `TestOverlay_AllTargetsIncludeCoreAndDomain`, the all-seven-target integrity suite, prune matrices, and full repository pass exercise those shared contracts.
- [✓] Grok creates no substitute harness content — dedicated `runGrok` writes only native Grok paths plus the shared root instruction; `TestGrokProjectInstallUsesNativeLayout` and the reported real isolated install assert no `.claude`, `.ai`, or `.grok/commands` output.
- [✓] Applicable propagation and contracts cover all seven targets — the target registry, native instruction, content, routing, Attention, integrity, inventory, contract, overlay, prune, satellite, prompt, doctor, and upgrade matrices include Grok; the active tripwire names all seven targets, and the full repository suite passed for the existing targets as well.
- [✓] Installed Grok loader reports native Hero origins — the reported Grok Build 1.0.4 `inspect --json` exercise found the project instruction, `.grok/agents`, canonical `spec-format`, user-invocable `command-design`, and Hero MCP from `.grok/config.toml` without a model request.
- [✓] Validation and closing evidence are complete for audit handoff — provided evidence records focused and full Go test passes, `git diff --check` pass, 16/16 Hero drift coverage, a complete ledger, and this independent SHIP audit; `hero spec verify grok-build-harness-target --skip-tests` is the post-audit closing command.

## Changes

- [✓] Native Grok target and shared workflow renderer — `internal/install/target_grok.go`, `internal/install/install.go`, `internal/install/render.go`, `internal/install/target_codex.go`, and `internal/install/agents_md.go` implement the dedicated native target and shared commands-as-skills path with exact Codex regression coverage.
- [✓] Byte-preserving native TOML MCP management — `internal/install/mcp.go` adds Grok project/global registration, validation, managed-block convergence, legacy deduplication, workspace-root args, and managed removal; dedicated tests cover each behavior.
- [✓] Full install lifecycle parity — CLI install/doctor/upgrade/uninstall and install state, detection, auto-sync, inventory, contract, pruning, and satellite registries all contain Grok-specific routing or declarative coverage.
- [✓] Contract and regression coverage — `internal/install/grok_test.go` and `internal/cli/grok_uninstall_test.go` add focused behavior tests, while all-target matrices and CLI baselines were expanded to seven targets.
- [✓] Guidance, docs, and tripwire — `README.md`, `GETTING-STARTED.md`, `domains/engineering/AGENTS.md`, prompt baselines, and `.hero/knowledge/tripwires/harness-changes-cover-all-targets/spec.md` describe and enforce native Grok behavior.
- [✓] Real Grok loader qualification — provided exercise evidence records two isolated installs (122 files then zero copies), native-origin `grok inspect --json` findings, absence of substitute layouts, and provenance-safe uninstall.

## Audit notes

- None.
