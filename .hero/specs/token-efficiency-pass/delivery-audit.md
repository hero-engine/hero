# Delivery audit — token-efficiency-pass

**Audited:** `git diff HEAD` (uncommitted working tree, this worktree)
**Verdict:** SHIP
**Surface:** noteworthy

## Acceptance criteria

- [✓] Every Changes file at/under target word count (±10%), two disclosed outliers — `wc -w` re-run on all 19 named files; matches the ledger's before/after table exactly (e.g. deliver.md 1597, feature-delivery-lead.md 1687, diagnose.md 509, peer.md 632, resume.md 417, brownfield 380/greenfield 370/architecture-reviewer 279, pm/AGENTS.md 2259). `buyer-researcher.md` (826, target ~600) and `story-writer.md` (1383, target 1375 ceiling) are outliers; both investigated below — legitimate, not rationalizations.
- [✓] Exactly one owning file per displaced rule, pointer everywhere else — spot-checked 6+ of the largest cuts by reading both sides (see Changes section below); every one held up.
- [~] Rule inventory recorded per file — the ledger states "subagent reports (not re-pasted here for length) enumerated cut rules" — the per-file inventories referenced by the ledger are not present in the spec file or elsewhere on disk I could find; I verified content preservation independently instead (by reading both sides of 10+ pointers), which substitutes for but doesn't equal the promised inventory artifact.
- [✓] `batch-discipline/SKILL.md` ships (497 words), referenced by deliver.md's Multi-spec mode and diagnose.md's batch path — both commands' relevant sections load it (`domains/engineering/commands/deliver.md:89`, `domains/engineering/commands/diagnose.md:37`).
- [✓] `buyer-research/SKILL.md` ships (634 words) with the 8-dimension research catalog, loaded by buyer-researcher.md's "Required skills" list (`domains/sales/agents/buyer-researcher.md:23`).
- [✓] Architectural stance/scale-readiness/strict rules stated only in `architecture-principles` (474 words); brownfield (380w), greenfield (370w), architecture-reviewer (279w) each keep 2-3 agent-specific bullets + a load pointer — all three verified read in full.
- [✓] `deliver.md`'s definition of done stated exactly once — `grep -c "Definition of done" deliver.md` = 3 (one heading, two "(see ... above)" pointers); content itself appears once (lines 20-35).
- [✓] Exactly one `✓/✗/~` legend in `delivery-audit` (verified: only the "Output format" copy at line ~100 remains, the "Surface decision rules" duplicate was cut); exactly one `## Drift handling` section in `spec-sizing` (verified: the old `## What to do on drift` section is gone, its content merged into `## Drift handling`, nothing dropped).
- [✓] "Ground before you guess" only in core `agent-reliability` (byte-identical text confirmed at `core/skills/agent-reliability/SKILL.md:25`); `debugging-investigation/SKILL.md:19` is a one-line pointer.
- [~] Does NOT modify `platform-delivery-lead.md`, `domains/engineering/AGENTS.md`, ledger contract, or chat-pack files — `platform-delivery-lead.md` and `core/skills/completion-ledger/SKILL.md` confirmed untouched (empty diffs); no chat-pack file in the diff. `domains/engineering/AGENTS.md` WAS touched, exactly as disclosed: one word (`batch-discipline,`) added to the existing "Delivery & spec process" line, mirrored in `internal/install/agents_md.go`. Both `TestEngineeringAgentsMdRosterComplete` and `TestEngineeringPackBodyMatchesGoFallback` pass on a fresh (uncached) run. This is a disclosed, mechanically-forced, minimum-scope deviation — treated as ✓-with-disclosure per the ledger's own flag.
- [✓] Cut sentence's rule ported to its home in the same commit if absent — verified for the investigator doctrine (agent-reliability gained a new "Dead ends in investigation work" section verified present), the MEDDPICC question banks (discovery-questioning has the full per-dimension bank), and the five-adjective test (moved from prd-structure/prd-anti-patterns into pitch-writing-shape-up, which is now the sole copy).
- [✓] Report per-file before/after word counts against baseline — Changes table below; all figures independently re-measured with `wc -w` and match the ledger's claims exactly.

## Changes

- [✓] 1. Create `batch-discipline/SKILL.md` — new file, 497 words, proper frontmatter (name/description/metadata, no `compatibility:` key). Covers concurrency discipline, per-item isolation, after-completion verification, summary-table format, failure handling.
- [✓] 2. Create `buyer-research/SKILL.md` — new file, 634 words, proper frontmatter, no `compatibility:` key. Full 8-dimension catalog (overview, tech stack, news, pain signals, buying triggers, org/stakeholder map, mutual connections, ICP fit) present and matches buyer-researcher.md's former subsections.
- [✓] 3. Expand `architecture-principles/SKILL.md` — 273→474 words. Read in full: core stance, scale-readiness rules, and guardrails (no-CQRS/event-sourcing/plugin-systems/workflow-engines) all present; this is the sole copy the three architecture agents point to.
- [✓] 4. `diagnose.md` batch-mode cut — 908→509. Batch section now states selection rules + a `batch-discipline` pointer for concurrency/isolation/verification/format; single-bug path untouched.
- [✓] 5. `deliver.md` four cuts — 2678→1597 (1081 saved). DoD stated once (grep confirms). Cold-audit pass compressed to invocation + verdict routing, with an explicit "the skill defines the report format ... do not restate it here" — verified `delivery-audit` skill still carries the AUDIT_VERDICT/HEADLINE/HIGHLIGHTS formats, display policy, signal-preservation rule. Ledger validation compressed to DONE/PARTIAL/SKIPPED routing + pointer — verified `completion-ledger` skill (untouched, sibling-owned) fully carries the contract, status definitions, and validation rules. Batch/Queue merged into one `## Multi-spec mode` heading (confirmed old headings gone).
- [✓] 6. `peer.md` cut — 979→632. Prompt-composition, budget defaults, and "What NOT to do" content compressed to pointers; verified `cross-repo-peering` skill fully carries the decision tree, prompt-composition guidance, budget defaults, and anti-patterns list that peer.md now only references.
- [✓] 7. `resume.md` trim — 610→417 (core-only). "What to say" trigger list now 6 phrases (was 18 per audit); "Why this matters" is one sentence; no closing "run unconditionally" paragraph remains. Pure in-file dedup, no external home needed — correct per spec.
- [✓] 8. `feature-delivery-lead.md` four compressions — 2697→1687 (1010 saved). Sizing-nudge → pointer to `spec-sizing` (verified paste-ready phrasing exists there). Ledger validation → halt condition + pointer to `completion-ledger` (verified). Cold-audit → invocation + verdict routing + pointer to `delivery-audit` (verified). Challenge-handling → trigger/halt kept, mode detection/protocol deferred to `challenge-diagnosis` (skill exists in engineering pack).
- [✓] 9. Architecture trio — brownfield 611→380, greenfield 605→370, architecture-reviewer 337→279. All three read in full: each keeps 2-3 agent-specific bullets plus an explicit `architecture-principles` load instruction; reviewer's previously-missing pointer is now present.
- [~] 10. `buyer-researcher.md` — 1366→826 (target ~600, 37.6% over strict ±10%). Company-research subsections 1-8 replaced with a load instruction + one-line summary (verified — file read in full). The outlier's justification (protected content — person research, output templates, writing-to-disk, rules, required-skills, intro — is everything but the cut section, ~791 words by my independent recount, close to the ledger's "~776") holds up: the 600-word target was set assuming subsections 1-8 were the bulk of the file, which they weren't after also accounting for the sections item 10 explicitly says to keep untouched. Legitimate, not a rationalization.
- [~] 11. `story-writer.md` — 1504→1383 (0.6% over a 1375 ceiling). One EARS example kept, full pattern set pointer to `acceptance-criteria-ears` (verified all 5 EARS forms present there); v1 filename note moved to `domains/pm/agents/README.md` (confirmed present). Negligible overage, disclosed.
- [✓] 12. Investigator doctrine — pm-investigator 1183→1175, debug-investigator 1596→1488. Both now point to a new `## Dead ends in investigation work` section in `core/skills/agent-reliability/SKILL.md` (verified — the section is substantively equivalent to what was cut from both agents, not a thin pointer).
- [✓] 13. `debugging-investigation/SKILL.md` — 685→604. "Ground before you guess" paragraph now byte-identical only in `agent-reliability`; one-line pointer left in place.
- [✓] 14. `spec-sizing/SKILL.md` — 2118→2018. The two drift-handling sections merged into one `## Drift handling`; read the merged section — leaf drift, container drift, "bump is the lead's job" rule, and "don't ignore drift" framing are all present, nothing dropped.
- [~] 15. `delivery-audit/SKILL.md` — 1722→1691 (31 words, exactly the size of the one legend block removed). The ledger additionally claims "removed an adjacent duplicated headline-policy paragraph found during the same pass" — the actual diff has exactly one hunk (the Legend block, 5 lines), and there is only one occurrence of the Headline/signal-preservation language in the file. **This sub-claim in the ledger does not match the diff** — minor overclaim, no content was actually lost or duplicated to begin with, so it doesn't cost a rule, but it's an inaccurate ledger statement worth flagging.
- [✓] 16. PM pack dedup (5 sub-clusters) — spot-checked all 5: cycle-planning/pitch-writing-shape-up (betting-table mechanics verified present in cycle-planning, appetite/cooldown/betting-table detail cut from pitch-writing-shape-up with working pointers); prd-structure & prd-anti-patterns (five-adjective test and Appetite/Rabbit-Holes/No-Gos examples moved into pitch-writing-shape-up as sole owner, confirmed); roadmap-framing/cross-domain-graph-query (reconciliation policy consolidated into roadmap-framing, cross-domain-graph-query now points at it, no state-transition rule dropped — actually clarified into more explicit bullets); OST/continuous-discovery-cadence (tree mechanics vs. weekly cadence cleanly split, cross-referenced both ways); PM "mission — principle #N" closers removed from cycle-planning, metrics-design, prioritization-frameworks, sprint-planning, roadmap-framing, prd-structure, prd-anti-patterns, pitch-writing-shape-up (8 files, matches "~10 files" claim within rounding) — confirmed `domains/pm/mission.md` is never given a resolvable path from any of them, and the one place that does resolve (`domains/pm/commands/roadmap.md:31`, a proper relative link) was correctly left untouched.
- [✓] 17. Sales pack dedup — "What this skill covers" confirmed absent from all 7 sales skills (grep returns zero matches repo-wide); frontmatter `description:` retained in each. `deal-qualification` question banks reduced to one exemplar + pointer, full banks confirmed present in `discovery-questioning` (`## Question Banks by MEDDPICC Dimension`); "Signs of a true champion" now points to `deal-strategy`'s `## Champion Development` section, which carries the 5-point definition and the champion test verbatim.
- [✓] 18. `pm/AGENTS.md` — 2587→2259 (documented baseline drift from a sibling landing first, verified plausible). Handoff-flip section now a pointer to `handoff-protocol` (read in full — pre-flight gates, owner-flip mechanics, bitemporal history, hand-back path, and the `hero_read_spec` detail are all present). Methodology-presets section shortened with a `pm-preset-detection` pointer. Second vocabulary table folded into the main routing table as a "Vocabulary-variant phrasing" column (confirmed no second table remains). "Capture execution plans" tightened in-file; confirmed no `plan-capture` skill exists anywhere in the repo, so the "no fake pointer invented" claim is accurate.
- [✓] 19. Scrubber startup boilerplate — SKIPPED, exactly as the spec permits ("skip without ceremony"). Confirmed zero diff on all `*-scrubber.md` files.

## Open items

- Ledger row 10 / Changes item on `domains/engineering/AGENTS.md` — **PARTIAL, but concrete reason**: one word added to an existing roster line, forced by the pre-existing `TestEngineeringAgentsMdRosterComplete` test (verified: this test fails without the change, since `batch-discipline` is a newly-created skill in the engineering pack). Disclosed prominently in the ledger's Preamble, not buried. Assessment: **concrete**, correctly the minimum edit, correctly mirrored in the Go fallback (`internal/install/agents_md.go`), both named tests pass on a fresh run. This is the right call.

## Audit notes

- **Ledger overclaim (item 15, minor):** the claim "also removed an adjacent duplicated headline-policy paragraph" in `delivery-audit/SKILL.md` is not reflected in the diff — the file has exactly one hunk (the Legend block, 31 words, matching the stated word delta exactly). No content was actually lost either way (nothing was duplicated to remove), so this doesn't cost a rule, but the ledger's evidence description for this row is not accurate. Flagging per the "no evidence found → say so" discipline, not because it changes the outcome.
- **Undisclosed asymmetry with the sales pack roster:** `domains/sales/skills/buyer-research/SKILL.md` is a new skill (item 2) but is **not** listed in `domains/sales/AGENTS.md`'s "### Skills Reference" table (still only the original 7 skills). This is the same category of gap that forced the disclosed `domains/engineering/AGENTS.md` edit — except there is no `TestSalesAgentsMdRosterComplete`-equivalent test to catch it, so it went unnoticed and undisclosed. It does not violate any stated acceptance criterion (the skill *is* ship-loaded correctly via buyer-researcher.md's own "Required skills" list, independent of the AGENTS.md roster table), and per this spec's own Boundaries, AGENTS.md roster completeness belongs to sibling `routing-file-completeness` — so leaving it alone is arguably in-scope-correct. Still worth a note: the ledger disclosed the engineering-side version of this gap but not the symmetric sales-side one, which reads as an inconsistency in how thoroughly parallel gaps were surfaced.
- **Rule-inventory artifact not found on disk.** The ledger states the per-file before/after rule inventories were done by subagents and "not re-pasted here for length," but I could not locate them anywhere in the spec file, `.hero/planning/initiatives/content-remediation/token-efficiency-pass/`, or elsewhere. I substituted independent verification (reading both sides of 10+ named pointers) for the promised inventory artifact, and every substantive check passed — so this doesn't sink the verdict, but the durable "rule inventory per file" the spec's Approach #3 calls a mandatory deliverable does not appear to exist as a discrete artifact.
- **Everything else checked out clean.** `go build ./...` and `go test ./...` are green; `TestEngineeringAgentsMdRosterComplete` and `TestEngineeringPackBodyMatchesGoFallback` pass on an uncached rerun. All 19 word counts independently re-measured match the ledger exactly. Every spot-checked pointer (11 of them, well beyond the "6 largest" the audit brief asked for) resolved to a real, substantively-complete home in the same install surface as the pointing file. No `compatibility:` key on either new skill. `platform-delivery-lead.md`, the ledger contract, and chat-pack files are genuinely untouched. This is a well-executed, honestly-reported pass — the two things flagged above are minor and neither represents a lost rule.
