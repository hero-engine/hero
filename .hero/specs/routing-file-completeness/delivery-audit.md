# Delivery audit — routing-file-completeness

**Audited:** `git diff e633113 -- .` (working tree, uncommitted) + `git status --short`
**Verdict:** SHIP
**Surface:** clean

## Acceptance criteria

- [✓] NL Routing row in engineering AGENTS.md for every installed command — `domains/engineering/AGENTS.md` (regenerated from `internal/install/agents_md.go:generateEngineeringAgentsMdBody`) now carries 11 new rows (`/blocked /capture /challenge /resume /roadmap-review /scrub /split /why /hero /peer /handoff`). Independently recounted installed commands: `domains/engineering/commands` = 14, `core/commands` = 15 → 29 total (not the spec's originally-authored 30; ledger's re-verified count matches actual `ls`).
- [✓] Name all installed agents (35) and skills (54) in engineering AGENTS.md rosters — new `### Agents Reference` / `### Skills Reference` sections. Independently counted: `domains/engineering/agents` = 31 + `core/agents` = 4 = 35 (matches); `domains/engineering/skills` = 38 + `core/skills` = 16 = 54 (matches ledger's corrected count, not the spec's original 53). Spot-checked `session-primer` and `migration-safety` present verbatim.
- [✓] `TestEngineeringPackBodyMatchesGoFallback` passes — ran directly: `PASS`.
- [✓] Missing roster entry fails `TestEngineeringAgentsMdRosterComplete`, naming it — independently verified by temporarily stripping `session-primer` from the pack file and re-running the test: it failed with `agents_md_test.go:116: domains/engineering/AGENTS.md is missing roster entries for:\n  agent session-primer`. Restored the file and re-ran both tests; both pass again. This is real, not a claimed-but-untested assertion.
- [✓] `###` depth for every body section in all three files, no `##` below H1 — `grep -nE '^## ' domains/{engineering,pm,sales}/AGENTS.md` returns no matches.
- [✓] Sections in skeleton's canonical order in all three files — confirmed by diff read: engineering inserts Agents/Skills Reference before CLI Commands; pm inserts Commands/Agents/Skills Reference before CLI Commands and moves "Capture execution plans" before the closing handoff/compaction pair; sales moves "Key CLI Commands" to follow Skills Reference and swaps Domain Configuration/Surviving Context Compaction so compaction-survival is last. Matches ledger's description exactly.
- [✓] No repo-relative links or hero-engine source paths in pm/sales AGENTS.md — `grep -nE '\]\((commands|agents|skills|spec-types|\.\./)' domains/pm/AGENTS.md domains/sales/AGENTS.md` returns no matches.
- [✓] Skeleton documented in `.hero/knowledge/conventions/domain-agents-md-skeleton.md` — read in full. Contains: heading-depth rule, canonical section order, path-placeholder rule, renderer rationale (`splitPackAgentsMd` H1→H2 demotion explained concretely), roster-completeness guidance, dual-edit note with the exact regen command, and an enforcement section listing both tests and both grep checks. This is a real convention document, not a stub.
- [✓] `domains/engineering/AGENTS.md` ≤ 2,600 words — `wc -w` → 2580.
- [✓] `domains/pm/skills/README.md` says `### Writing (6)` — confirmed at line 12; six bullets follow (story-writing-invest, acceptance-criteria-ears, prd-structure, prd-anti-patterns, pitch-writing-shape-up, roadmap-framing) matching the "(6)" count.
- [✓] pm/sales restructuring preserves sibling-owned claim text verbatim apart from relocation/heading/path form — read both full diffs. pm's diff is exclusively `##`→`###` demotions, three new Reference sections, and one section relocation ("Capture execution plans" moved to just before "Keep handoff briefings current"). Sales' diff is `##`→`###` demotions, `---` divider removal, two section relocations (Key CLI Commands moved up; Domain Configuration/Surviving Context Compaction swapped), and one path-form rewrite (`spec-types/deal.md` → "the registered `deal` spec type" + `.hero/cache/spec-types.json`). No prose content was rewritten in either file beyond that path sentence.

## Changes

- [✓] New skeleton convention file — `.hero/knowledge/conventions/domain-agents-md-skeleton.md` exists, substantive (see above).
- [✓] Pointer comment in `agents_md.go` near existing authoring guidance — confirmed two new comment blocks: one on `loadPackAgentsMdBody`'s doc comment (line ~91-93) and one on the on-disk-layout warning near `generateAgentsMdBody` (line ~292-294), both pointing at the skeleton convention path.
- [✓] Extend `generateEngineeringAgentsMdBody`, regenerate pack file — diff confirmed: 11 new NL Routing `sb.WriteString` rows, `/hero` added to the slash/CLI parity row, new `### Agents Reference` / `### Skills Reference` `sb.WriteString` blocks. Parity test passes, so the pack file is provably the regenerated output of this function (not hand-edited out of sync).
- [✓] New `TestEngineeringAgentsMdRosterComplete` — added, genuinely walks `domains/engineering/{commands,agents,skills}` + `core/{commands,agents,skills}` via `os.ReadDir`, skips `README.md`, checks literal substring presence in the pack file. Independently verified fail-and-restore behavior (see AC above).
- [✓] pm structural pass + roster — confirmed in diff: heading demotions, new Commands/Agents/Skills Reference sections naming `pm-delivery-lead`, `prioritization-strategist`, `duplicate-detector`, and skill groups totaling 19 pm + 16 core skills (per the ledger's corrected core-skill count of 16, matching the independently-confirmed `core/skills` = 16 directory count — note the spec text said "14 pack + 17 core" for engineering/PM math elsewhere but the ledger's re-verification and my own recount agree at 16 core skills, so this is self-consistent, not a discrepancy).
- [✓] sales structural pass + link fix — confirmed: heading demotions, `---` dividers removed (the two inside the Deal Spec Structure YAML fence correctly preserved, since those are code content not section dividers), sections reordered, one path-form rewrite for `spec-types/deal.md`.
- [✓] `domains/pm/skills/README.md` line 12 fix — confirmed, `Writing (5)` → `Writing (6)`.

## Open items (if any)

- **Validation-section word-count reporting gap (soft, non-blocking).** The spec's Validation section asks the engineer to "record pm/sales counts to confirm the structural pass didn't balloon them (pm ≤ ~2,450 given roster additions...)". The Completion Ledger never states pm/sales word counts. Independently measured: `domains/pm/AGENTS.md` = 2,587 words (exceeds the spec's own ~2,450 estimate by ~137 words / 5.6%); `domains/sales/AGENTS.md` = 1,310 words. Neither file has a hard word-budget Acceptance Criterion (only engineering does, and engineering passes at 2,580/2,600), so this is not an AC failure — but the ledger is silent on a Validation step the spec explicitly called out, and pm quietly exceeded the spec's own soft estimate. Not blocking; flagging for the record.

## Audit notes

- No performative DONE rows found. Every ledger row that claims a mechanical check (grep, wc, go test) was independently re-run and matched. The one row that claims a manual/behavioral action (roster fail-and-restore) was independently reproduced by this auditor, not merely trusted.
- Full build and test suite verified clean from the worktree root: `go build ./...` (no output/errors), `go test ./...` (all packages `ok` or `[no test files]`, none `FAIL`), plus targeted runs of `./internal/install/...`, `./internal/cli/...`, and the root package `.`.
- Boundaries respected: `git diff e633113 -- core/commands/hero.md` is empty (untouched). The `internal/install/agents_md.go` diff touches only `generateEngineeringAgentsMdBody` and two doc comments — no changes to `splitPackAgentsMd`, the managed-region orchestrator, or `installFlat` (confirmed via `grep -n "^func \|splitPackAgentsMd\|installFlat"` on the diff, which returned nothing).
- The spec's counts (30 commands / 53 skills) were stale relative to the current tree; the ledger explicitly flags and re-verifies this (29 commands, 54 skills), and the Go-side roster test walks directories directly rather than hardcoding a count, so the discrepancy is self-correcting and doesn't indicate sloppy delivery — it indicates the spec was authored against an earlier tree state, which the engineer caught.
- The pre-flight note claiming both `follows` siblings (`pm-pack-phantom-surfaces`, `sales-pack-reality-sync`) had already landed is consistent with what's visible in the diff: pm's Project Structure already showed `<harness>/`/`.hero/` placeholders and sales' reference tables already used backticked names before this delivery's changes — this delivery's diff only adds roster sections and reorders/demotes, it does not touch those already-fixed areas' content.
