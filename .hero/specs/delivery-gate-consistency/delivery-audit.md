# Delivery audit — delivery-gate-consistency

**Audited:** working-tree diff against base `752e5169301c802a9a2e615bbce438239586853f` (HEAD == base; no new commits, changes are uncommitted) — `git diff 752e5169301c802a9a2e615bbce438239586853f -- .`
**Verdict:** SHIP
**Surface:** clean

## Acceptance criteria

- [✓] Ledger format contract lives in exactly one file (`core/skills/completion-ledger/SKILL.md`), cited by engineer.md/feature-delivery-lead.md/deliver.md/delivery-audit/agent-reliability — new file exists with the format block, status definitions, honesty rules, "What Gate 1 actually parses," "Validating a ledger." All 5 citing files updated and individually greppable: `core/skills/agent-reliability/SKILL.md:41`, `domains/engineering/agents/engineer.md` (compressed section), `domains/engineering/agents/feature-delivery-lead.md:124`, `domains/engineering/commands/deliver.md:103,157`, `domains/engineering/skills/delivery-audit/SKILL.md:38`. Cross-checked: no other shipped `.md` file under `core/` or `domains/` restates the format (`rg "THE SYSTEM SHALL \.\.\. \| DONE"` hits only the new skill). Note: the engineer's own grep claim ("returns exactly one hit across all `.md` files") is imprecise — the literal table header `Criterion (abbreviated)` also appears in ~10 unrelated specs' own Completion Ledger instances under `.hero/specs/` and `.hero/planning/` (including this spec's own ledger), which `rg` only surfaces with `--hidden`. This is harmless (those are ledger *instances*, not format *documentation*) but the grep description as literally stated is not accurate. Does not affect the AC's substance.
- [✓] Skill documents `[signed-off]` annotation, UNKNOWN-status failure mode, advisory exercise-check status, matching `ledger.go`/`checkLedger` — independently verified line-by-line against `internal/spec/ledger.go` (`ParseLedger`, `parseLedgerContent`, `parseDataRow`, `parseStatus`) and `internal/cli/verify.go:208-288` (`checkLedger`). Wrote and ran an independent scratch Go test (not the engineer's, a fresh one) pasting the skill's exact format example through `Parse`/`ParseLedger`: 4 AC rows and 2 Changes rows parsed correctly, `SKIPPED` + `[signed-off]` row correctly flagged `SignedOff: true`, exercise checkbox correctly parsed as checked with detail. Test file removed after use, confirmed not in diff.
- [✓] platform-delivery-lead delivery routes through the identical closing sequence as feature-delivery-lead, no hand-edit/move-to-specs instruction — `domains/engineering/agents/platform-delivery-lead.md:61-71` "Delivery phase" replaced with a citation block naming the exact file/section ("feature-delivery-lead's 'Delivery phase' verbatim... steps 1-21... same `hero spec verify` close") + 3 delta bullets (migration sequencing, migration-engineer, brownfield-architect) containing no procedural steps. `rg "move the spec from|update its status"` against the file returns empty (verified independently).
- [✓] `/sprint` execute/run/go routes initiative → `/drive`, ad-hoc list → `/deliver` queue mode, no bespoke loop — `domains/engineering/commands/sprint.md:33-44` "## Execute mode" (33 old lines) replaced with "## Executing the sprint" (12 lines), routing exactly as specified. Planning mode (lines 1-31, steps 1-7 + principles) verified byte-for-byte untouched.
- [✓] No engineering-pack/core content instructs `hero spec complete` on a work spec or hand-editing `status: completed` (diagnose.md/debug-investigator.md excepted) — `rg -n "hero spec complete" core/ domains/engineering/ --glob '*.md'` returns exactly: the new skill's prohibition sentence, sprint.md's prohibition reference, feature-delivery-lead.md's prohibition reference, and the two excepted files (`diagnose.md`, `debug-investigator.md`). No instruction to run it on a work spec remains.
- [✓] `hero install` for any domain installs `completion-ledger`, resolving `agent-reliability`'s reference — independently rebuilt the `hero` binary from this worktree and ran `hero install project . --target claude --domain pm --root` and `--domain engineering --root` against fresh scratch directories (not reusing the engineer's). Confirmed in both: `.claude/skills/completion-ledger/SKILL.md` installs, and `.claude/skills/agent-reliability/SKILL.md:41` correctly reads "see the `completion-ledger` skill for the format." Scratch dirs cleaned up after verification.
- [✓] `go test ./...` passes after delivery, including `content_parity_test.go` and the AGENTS.md identity test — ran independently: full suite green (all ~90 packages `ok`/no-test-files, exit 0). `go test . -run TestDomainPacks_NoUnannotatedCoreShadows -v` passes all 3 subtests (engineering/sales/pm). `go test ./internal/install/... -run TestEngineeringPackBodyMatchesGoFallback -v` passes.

## Changes

- [✓] Create `core/skills/completion-ledger/SKILL.md` — confirmed new file (untracked, `??` in git status). Frontmatter matches spec exactly: `name: completion-ledger`, correct description, `compatibility: opencode`, `metadata: {audience, purpose}`. Body contains all specified subsections.
- [✓] Edit `engineer.md` — compress Closing output section — diff confirms lines 104-169 (the ~65-line format/status/rules/preamble block) replaced with a ~5-line mandate + skill citation + two non-negotiables (every-item-a-row; honest non-DONE beats performative DONE). Startup always-load list (line 22, outside the diff range) untouched.
- [✓] Edit `platform-delivery-lead.md` — thin delta — diff confirms lines 61-80 (14-line numbered procedure ending in "move the spec from planning/ to specs/") replaced with citation block + 3 delta bullets. Frontmatter (name/description/permission block), identity paragraph, Design phase, Rules, Default output all byte-identical to base — verified by reading the full post-diff file.
- [✓] Edit `sprint.md` — delete execute mode — confirmed via `-U0` diff: old 33-line "## Execute mode" + "Safety rails" block replaced with new 12-line "## Executing the sprint" section. Planning mode untouched.
- [✓] Edit `feature-delivery-lead.md` — gate-machinery citations — `-U0` diff confirms exactly 3 single-line replacements at lines 124, 136, 164 — precisely the step 17 citation, step 19 `hero spec verify` correction, and wrap-up step 1 repoint described in the spec. No other lines touched (stat: 6 changed lines total, 3 old + 3 new).
- [✓] Edit `deliver.md` — gate-machinery citations — `-U0` diff confirms exactly 3 single-line replacements at lines 103, 105, 157 — batch step 4's two edits and single-spec step 5's citation repoint. No other lines touched (stat: 6 changed lines total, 3 old + 3 new). PARTIAL/SKIPPED/BLOCKED routing sentence and auto-archive note preserved verbatim as required.
- [✓] Edit `core/skills/agent-reliability/SKILL.md` line 41 — single-line diff confirmed: "see the engineering pack's `engineer.md` for the format" → "see the `completion-ledger` skill for the format"; "packs without an engineer agent account..." caveat kept verbatim.
- [✓] Edit `domains/engineering/skills/delivery-audit/SKILL.md` line 38 — single-line diff confirmed: appended "(format contract: `completion-ledger` skill)" to the Ledger input bullet.

## Open items (if any)

None. No PARTIAL, SKIPPED, or BLOCKED rows in the ledger — all 7 AC rows and all 8 Changes rows are `DONE`.

## Audit notes

- **Boundaries respected, independently verified:**
  - `internal/spec/ledger.go`, `internal/cli/verify.go`, `internal/cli/complete.go` — `git diff` against base is empty (untouched, as required — read-only inputs).
  - `domains/engineering/AGENTS.md` and `internal/install/agents_md.go` — `git diff` against base is empty (untouched, as required).
  - `domains/sales/commands/debrief.md` — `git diff` against base is empty (untouched, as required, out of scope for this spec).
  - platform-delivery-lead's frontmatter (`name`, `description`, `permission` block) is byte-identical to base — no roster changes, README counts stay valid.
  - feature-delivery-lead.md and deliver.md carry *only* the specified gate-machinery line edits (3 lines each, confirmed via `-U0` diff) — no verbosity/compression drift into token-efficiency-pass's territory.
  - Diff footprint is exactly the 8 files the spec's Changes section names, plus the spec.md itself (ledger self-update). No scope drift.
- **`hero docs check` soft spot confirmed pre-existing:** independently reproduced via `git stash`/`git stash pop` — both pre-change and post-change trees report identical "actual counts: 0" behavior against README/GETTING-STARTED. This is a tool limitation unrelated to this delivery, matching the engineer's own characterization.
- **One documentation-precision nit** (not a defect, not downgrading anything): the engineer's ledger note for AC #1 claims a grep for the ledger table header "returns exactly one hit... across all `.md` files." In fact it returns ~11-12 hits when hidden directories (`.hero/`) are included — every spec that has ever had its own Completion Ledger (a normal, expected thing) reuses the same table header text, including this spec's own ledger at the bottom of `spec.md`. The AC's actual intent (no other file documents/forks the *format contract itself*) holds up under a more precise check (`rg "THE SYSTEM SHALL \.\.\. \| DONE"` → 1 hit, the skill). Worth a mental note for future ledger-grep precision, not a reason to hold this delivery.
- Test evidence was independently reproduced, not just trusted: full `go test ./...`, the two named tests individually, a from-scratch parse test (not reusing the engineer's removed file), and two from-scratch `hero install` runs against fresh scratch directories. All matched the ledger's claims.
