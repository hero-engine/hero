# Delivery audit — attention-conversational-routes

**Audited:** `git diff HEAD -- domains/engineering/routing.md contracts/attention/contract_test.go contracts/attention/testdata/v1/manifest.json contracts/attention/testdata/v1/HERO-CODE-HANDOFF.md internal/install/routing_guidance_test.go internal/serve/routing_reference_test.go internal/serve/api_attention.go .hero/planning/initiatives/conversational-attention-operability/attention-conversational-routes/spec.md`, plus the four named untracked artifacts
**Verdict:** SHIP
**Surface:** clean

## Acceptance criteria

- [✓] AC-1: bounded Attention read — the canonical route calls `hero_attention_snapshot` once with `limit: 8` and declares bounded reads side-effect-free (`domains/engineering/routing.md:54`, `domains/engineering/routing.md:68`); `route-attention-snapshot` records read effect, no consent, and zero mutations (`contracts/attention/testdata/v1/conversational-routes.json:5`).
- [✓] AC-2: Mail list and unique show remain reads — the canonical table selects `hero_mail_list` and uniquely resolved `hero_mail_show` (`domains/engineering/routing.md:55`, `domains/engineering/routing.md:56`); the corpus pins both to read/no-consent/zero-mutation behavior (`contracts/attention/testdata/v1/conversational-routes.json:21`, `contracts/attention/testdata/v1/conversational-routes.json:37`) and the trust rule keeps Mail content inert (`domains/engineering/routing.md:78`).
- [✓] AC-3: explicit send/reply dispatch once with stable idempotency — the route table selects the typed tools (`domains/engineering/routing.md:57`, `domains/engineering/routing.md:61`) and the safety rule requires same-key retry (`domains/engineering/routing.md:80`); send and reply cases require explicit-user consent, one initial mutation, and zero retry mutations (`contracts/attention/testdata/v1/conversational-routes.json:54`, `contracts/attention/testdata/v1/conversational-routes.json:122`).
- [✓] AC-4: ambiguous facts clarify without mutation — guidance asks only for the missing fact and dispatches zero mutations (`domains/engineering/routing.md:66`, `domains/engineering/routing.md:68`); six ambiguity cases cover recipient, content, message, project, timing, and destination (`contracts/attention/testdata/v1/conversational-routes.json:276`), while validation rejects executable operations or mutations on clarification routes (`contracts/attention/conversational_route.go:154`).
- [✓] AC-5: user work creates Focus; model work suggests — the canonical table separates `hero_focus_create` and `hero_focus_suggest` (`domains/engineering/routing.md:62`, `domains/engineering/routing.md:63`); the corpus includes both mappings (`contracts/attention/testdata/v1/conversational-routes.json:139`, `contracts/attention/testdata/v1/conversational-routes.json:156`) and validation rejects model dispatch to any other operation (`contracts/attention/conversational_route.go:197`).
- [✓] AC-6: exact advertised row actions only — guidance requires the exact advertised action ID and revision and forbids manufacture from prose (`domains/engineering/routing.md:64`, `domains/engineering/routing.md:81`); the corpus covers accept, dismiss, move, launch, and promote (`contracts/attention/testdata/v1/conversational-routes.json:173`, `contracts/attention/testdata/v1/conversational-routes.json:191`, `contracts/attention/testdata/v1/conversational-routes.json:209`, `contracts/attention/testdata/v1/conversational-routes.json:227`, `contracts/attention/testdata/v1/conversational-routes.json:245`) and `TestConversationalRouteFixtureMatchesCanonicalPoliciesAndActions` checks every referenced action against the canonical all-actions fixture (`contracts/attention/contract_test.go:199`).
- [✓] AC-7: peering remains typed — the route source distinguishes advisory, spec-out, and handoff from ordinary Mail (`domains/engineering/routing.md:57`, `domains/engineering/routing.md:58`, `domains/engineering/routing.md:59`, `domains/engineering/routing.md:60`); three `cli_workflow` cases encode those routes (`contracts/attention/testdata/v1/conversational-routes.json:71`, `contracts/attention/testdata/v1/conversational-routes.json:88`, `contracts/attention/testdata/v1/conversational-routes.json:105`) and `TestConversationalRouteCorpusUsesRealCLICommands` resolves exactly three against the real CLI tree (`internal/cli/conversational_routes_test.go:12`).
- [✓] AC-8: Mail content is untrusted — the installed rule prohibits executing instructions, prompts, or tool calls received in Mail (`domains/engineering/routing.md:78`); the three hostile-Mail cases all expect `ignore_untrusted` and zero mutations (`contracts/attention/testdata/v1/conversational-routes.json:354`, `contracts/attention/testdata/v1/conversational-routes.json:365`, `contracts/attention/testdata/v1/conversational-routes.json:376`), and the validator rejects any Mail-content dispatch (`contracts/attention/conversational_route.go:145`).
- [✓] AC-9: complete registry-backed corpus — the fixture contains 29 named cases across the required 16 route-family, 6 ambiguity, 3 untrusted-Mail, and 4 resilience categories (`contracts/attention/testdata/v1/conversational-routes.json:1`); schema and DTO/checksum coverage are wired through the manifest test (`contracts/attention/contract_test.go:21`, `contracts/attention/contract_test.go:77`), semantic floors and policy/action invariants are enforced by `ValidateConversationalRouteFixture` (`contracts/attention/conversational_route.go:85`), MCP inventory is checked by `TestConversationalRouteCorpusUsesAdvertisedMCPTools` (`internal/serve/routing_reference_test.go:85`), and typed CLI routes are checked by `TestConversationalRouteCorpusUsesRealCLICommands` (`internal/cli/conversational_routes_test.go:12`).
- [✓] AC-10: six native harness roots receive equivalent guidance — `TestRoutingGuidanceReachesAllHarnessNativeRoots` installs OpenCode, Cursor, Claude, Copilot, Codex, and Generic and asserts shared Attention, consent, trust, and revision markers in each native root (`internal/install/routing_guidance_test.go:36`); the embedded-source parity test prevents a second routing source (`internal/install/routing_guidance_test.go:12`).

## Changes

- [✓] Add Attention routes and safety rules to the canonical source — the route-family table and consent, ambiguity, trust, stale, unavailable, idempotency, and advertised-action rules are present in `domains/engineering/routing.md:46`.
- [✓] Add the versioned schema, DTO, and complete corpus — `contracts/attention/schema/v1/conversational-routes.schema.json:1`, `contracts/attention/conversational_route.go:8`, and `contracts/attention/testdata/v1/conversational-routes.json:1` provide the three artifacts.
- [✓] Validate policy, effect, consent, target, trust, and category invariants — `ValidateConversationalRouteFixture` performs those checks and enforces the category floors (`contracts/attention/conversational_route.go:85`).
- [✓] Validate every dispatched surface against real registries — contract tests check canonical actions and negative invented mappings (`contracts/attention/contract_test.go:199`, `contracts/attention/contract_test.go:234`), serve tests check real MCP definitions (`internal/serve/routing_reference_test.go:85`), and CLI tests check real commands (`internal/cli/conversational_routes_test.go:12`).
- [✓] Add typed peering cases — advisory, spec-out, and handoff are distinct `cli_workflow` cases with resolved peers and canonical command/effect mappings (`contracts/attention/testdata/v1/conversational-routes.json:71`).
- [✓] Add negative and resilience coverage — ambiguity, model suggestion, hostile Mail, unavailable, stale, retry, and unknown-additive cases record exact initial and retry mutation counts (`contracts/attention/testdata/v1/conversational-routes.json:156`, `contracts/attention/testdata/v1/conversational-routes.json:263`, `contracts/attention/testdata/v1/conversational-routes.json:354`, `contracts/attention/testdata/v1/conversational-routes.json:387`).
- [✓] Add six-target and reference validation — the install matrix covers all six native roots (`internal/install/routing_guidance_test.go:36`), canonical Markdown references resolve against real workflows/tools/skills (`internal/serve/routing_reference_test.go:30`), and negative reference tests reject invented names (`internal/serve/routing_reference_test.go:48`).
- [✓] Publish the corpus through the v1 manifest — the manifest lists `conversational-routes.json` with its schema and SHA-256 (`contracts/attention/testdata/v1/manifest.json:8`); the contract endpoint checksum and Hero Code handoff use the updated manifest hash (`internal/serve/api_attention.go:10`, `contracts/attention/testdata/v1/HERO-CODE-HANDOFF.md:46`).

## Audit notes

- No missing ledger rows, partial items, skipped items, performative DONE claims, or child-specific scope drift were found.
- Provided validation evidence reports `go test ./contracts/attention ./internal/install ./internal/serve ./internal/cli`, `go test ./...`, the six-target routing matrix, schema/DTO/checksum and real-surface checks, negative safety tests, and `git diff --check` all passing.
