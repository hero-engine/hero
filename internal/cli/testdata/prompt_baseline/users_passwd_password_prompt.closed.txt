$ hero admin users passwd alice
stdin-condition: closed
exit: 1
--- stdout ---
--- stderr ---
hero: minted peer_id for cross-repo peering (recorded in events.log)
hero: 1 legacy install artifact(s) detected — Claude Code / Codex / Cursor will silently fail to load Hero skills and agents.
hero: run 'hero upgrade' (or 'hero install <mode> .') to clean up.
Error: cannot read a password without a terminal: Hero will not accept a password from a pipe, file, or here-doc. Run this command from an interactive terminal, or set the password non-interactively at creation time with `hero admin users add --password <value>`
Usage:
  hero admin users passwd <username> [flags]

Flags:
  -h, --help   help for passwd

Global Flags:
      --smoke   run smoke verification for this command and exit

cannot read a password without a terminal: Hero will not accept a password from a pipe, file, or here-doc. Run this command from an interactive terminal, or set the password non-interactively at creation time with `hero admin users add --password <value>`
